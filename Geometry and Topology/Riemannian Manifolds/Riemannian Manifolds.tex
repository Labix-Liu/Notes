\documentclass[a4paper]{article}

\input{C:/Users/liula/Desktop/Latex/Headers/Headers.tex}

\pagestyle{fancy}
\fancyhf{}
\rhead{Labix}
\lhead{Riemannian Manifolds}
\rfoot{\thepage}

\title{Riemannian Manifolds}

\author{Labix}

\date{\today}
\begin{document}
\maketitle
\begin{abstract}
\end{abstract}
\pagebreak
\tableofcontents
\pagebreak

\section{Riemannian Metrics}
\subsection{The Riemannian Metric}
\begin{defn}{Riemannian Metric}{}\\
Let $M$ be a smooth manifold. A Riemannian metric on $M$ is a smooth section $$g\in\Gamma^\infty((TM\otimes TM)^\ast)$$ such that $g_p:T_p(M)\times T_p(M)\to\R$ is an inner product for all $p\in M$. 
\end{defn}

Equivalently, a Riemannian metric is an assignment $p\mapsto g_p$ such that for any smooth sections $s,t\in\Gamma(M,TM)$, the map $p\mapsto g_p(s(p),t(p))$ is smooth. 

\begin{defn}{Riemannian Manifold}{}\\
A Riemannian manifold $(M,g)$ is a manifold $M$ together with a Riemannian metric $g$ on $M$. 
\end{defn}

Given a $(p,q)$-tensor field $F$ of a smooth vector bundle, recall that at a local frame $(U,s_1,\dots,s_k)$ the components of $F$ are the smooth functions $F^{i_1,\dots,i_p}_{j_1,\dots,j_q}$ such that $$F=\sum_{1\leq i_1,\dots,i_p,j_1,\dots,j_q\leq k}F^{i_1\cdots i_p}_{j_1\cdots j_q} s_{i_1}\otimes\cdots\otimes s_{i_p}\otimes s_{j_1}^\ast\otimes\cdots\otimes s_{j_q}^\ast$$ locally. When $E=TM$, we can recover the components by substituting: $$F^{i_1\cdots i_p}_{j_1\cdots j_q}=F\left(dx^{i_1},\dots,dx^{i_p},\frac{\partial}{\partial x^{j_1}},\dots,\frac{\partial}{\partial x^{j_q}}\right)$$ Notice that $g$ is a $(0,2)$-tensor field since $(TM\otimes TM)^\ast=TM^\ast\otimes TM^\ast$. So the components of $g$ are given by $$g_{ij}=g\left(\frac{\partial}{\partial x^i},\frac{\partial}{\partial x^j}\right)$$ In particular, for $p\in M$ and $v,w\in T_pM$, we have $$g_p(v,w)=\sum_{1\leq i,j\leq n}g_{i,j}\left(\frac{\partial}{\partial x^i}\bigg{|}_p,\frac{\partial}{\partial x^j}\bigg{|}_p\right)(dx^i)|_p(v)(dx^j)|_p(w)$$

\begin{eg}{}{}\\
Define the metric $g$ on $S^n$ as follows. For $p\in S^n\subseteq\R^{n+1}$, we have $$T_pS^n=\{v\in\R^{n+1}\;|\;\langle p,v\rangle_{\R^{n+1}}=0\}$$ Then define $$g_p(v,w)=\langle v,w\rangle_{\R^{n+1}}$$ Consider the charts $(U_+,\phi_+=(x_1,\dots,x_n))$ and $(U_-,\phi_-=(x_1,\dots,x_n))$ where $U_+$ and $U_-$ are the nothern and southern hemispheres respectively. In terms of the coordinates in $\R^{n+1}$, the basis for the tangent space is given by pushing forward along the inverse of the coordinate charts, and this gives the basis $$e_i\mp\frac{x_i}{1-\abs{x}^2}e_{n+1}$$ for $1\leq i\leq n$ at the point $p\in\phi_{\pm}^{-1}(x)$ where $e_1,\dots,e_{n+1}$ is the standard basis vector of $\R^{n+1}$. Then we compute that $$g_{ij}=\delta_{ij}+\frac{x_ix_j}{1-\abs{x}^2}$$
\end{eg}

\begin{prp}{}{}\\
Every smooth manifold admits a Riemannian metric. 
\end{prp}

\subsection{Maps Preserving the Riemannian Metric}
\begin{prp}{}{}\\
Let $M,N$ be a smooth manifolds. Let $g$ be a Riemannian metric on $N$. Let $f:M\to N$ be a smooth map. Then $f^\ast g$ is a Riemannian metric on $M$ if and only if $f$ is an immersion. 
\end{prp}

This shows that if we want a map $f:M\to N$ that preserves Riemannian metric, we need $f$ to at least be an immersion. 

\begin{defn}{Isometric Embedding}{}\\
Let $(M_1,g_1)$ and $(M_2,g_2)$ be Riemannian manifolds. Let $f:M_1\to M_2$ be a smooth map. We say that $f$ is an isometric embedding if the following are true. 
\begin{itemize}
\item $f$ is an immersion. 
\item $f^\ast g_2=g_1$. 
\end{itemize}
\end{defn}

\begin{thm}{Nash}{}\\
Let $(M,g)$ be a Riemannian manifold of dimension $n$. Then there exists an isometric embedding $f:M\to\R^{(n+2)(n+3)/2}$ with the standard Euclidean metric. 
\end{thm}

\begin{defn}{Isometries}{}\\
Let $(M_1,g_1)$ and $(M_2,g_2)$ be Riemannian manifolds. Let $f:M_1\to M_2$ be an isometric embedding. We say that $f$ is an isometry if there exists an isometric embedding $h:M_2\to M_1$ such that $$h\circ f=\text{id}_{M_1}\;\;\text{ and }\;\;f\circ h=\text{id}_{M_2}$$ In this case, we say that $M_1$ and $M_2$ are isometric. 
\end{defn}

\begin{prp}{}{}\\
Let $(M_1,g_1)$ and $(M_2,g_2)$ be Riemannian manifolds. Let $f:M_1\to M_2$ be a smooth map. Then the following are equivalent. 
\begin{itemize}
\item $f$ is an isometry. 
\item $f$ is an isometric embedding and a diffeomorphism. 
\item $f$ is an isometric embedding that is bijective. 
\item $f$ is an isometric embedding that is surjective. 
\end{itemize}
\end{prp}

\begin{defn}{Local Isometries}{}\\
Let $(M,g)$ and $(N,h)$ be two Riemannian manifolds. We say that they are locally isometric if for all $p\in M$, there exists an open neighbourhood $U\subseteq M$ of $p$ and $V\subseteq N$ open and an isometry $f:U\to V$. 
\end{defn}

\begin{defn}{Flat Manifolds}{}\\
Let $(M,g)$ be a Riemannian manifold. We say that $(M,g)$ is flat if it is locally isometric to $\R^n$ with the standard metric. 
\end{defn}

In general, not every Riemannian manifold is flat. This can be shown once we discuss curvatures and torsions. However, this is true when $n=1$. 

\begin{lmm}{}{} Every 1 dimensional Riemannian manifold is flat. 
\end{lmm}

\subsection{The Musical Isomorphisms}
\begin{defn}{The Flat Map}{}\\
Let $(M,g)$ be a Riemannian manifold. Let $p\in M$. For each $X\in T_pM$, define the flat map $$\flat:T_pM\to T_p^\ast M$$ by sending $X\in T_pM$ to the map $X^\flat:T_pM\to\R$ by $X^\flat(Y)=g_p(X,Y)$. 
\end{defn}

\begin{thm}{The Musical Isomorphism}{}\\
Let $(M,g)$ be a Riemannian manifold. Let $p\in M$. Then the flat map $$\flat:T_pM\to T_p^\ast M$$ is an isomorphism. 
\end{thm}

\begin{defn}{The Sharp Map}{}\\
Let $(M,g)$ be a Riemannian manifold. Let $p\in M$. Define the sharp map $$\#:T_p^\ast M\to T_pM$$ to be the inverse of the flat map. 
\end{defn}

\begin{prp}{}{}\\
Let $(M,g)$ be a Riemannian manifold. Then the following are true. 
\begin{itemize}
\item The components of the flat map are given by $g_{ij}$. 
\item The components of the sharp map are given by elements of the inverse matrix $(g_{ij})^{-1}$. 
\end{itemize}
\end{prp}

We denote the components of the sharp map by $g^{ij}$. 

\subsection{The Induced Inner Product on Tensor Fields}
\begin{defn}{The Induced Metric on the Cotangent Bundle}{}\\
Let $(M,g)$ be a Riemannian manifold. Define the induced metric on $T^\ast M$ to be the section $g^{-1}\in\Gamma(M,(T^\ast M\otimes T^\ast M)^\ast)$ by $$g^{-1}(\alpha,\beta)=g(\alpha^\#,\beta^\#)$$
\end{defn}

\begin{prp}{}{}\\
Let $(M,g)$ be a Riemannian manifold. Then the components of $g^{-1}$ is given by the elements of the inverse matrix $(g_{ij})^{-1}$. 
\end{prp}

\begin{defn}{Inner Product of Tensors}{}\\
Let $(M,g)$ be a Riemannian manifold. Define the induced inner product on $\mT_p^q(TM)$ as follows. For each $x\in M$ and $v_1,\dots,v_p,w_1,\dots,w_p\in T_xM$ and $\alpha_1,\dots,\alpha_q,\beta_1,\dots,\beta_q\in T_x^\ast M$, define $$\langle v_1\otimes\cdots\otimes v_p\otimes\alpha_1\otimes\cdots\otimes\alpha_q,w_1\otimes\cdots\otimes w_p\otimes\beta_1\otimes\cdots\otimes\beta_q\rangle=\prod_{i=1}^pg_x(v_i,w_i)\cdot\prod_{j=1}^qg_x(\alpha_j,\beta_j)$$
\end{defn}

\begin{prp}{}{}\\
Let $(M,g)$ be a Riemannian manifold. Then the components of the induced inner product on $\mT_p^q(TM)$ is given by $$\langle-,-\rangle_{\substack{i_1,\dots,i_p,j_1,\dots,j_q\\k_1,\dots,k_p,l_1,\dots,l_q}}=g_{i_1k_1}\cdots g_{i_pk_p}g^{j_1l_1}\cdots g^{j_ql_q}$$
\end{prp}

\subsection{Riemannain Manifolds as a Metric Space}
\begin{defn}{Length of a Tangent Vector}{}\\
Let $(M,g)$ be a Riemannian manifold. Let $v\in T_p(M)$ be a tangent vector for $p\in M$. Define the length of $v$ to be $$\abs{v}_g=\sqrt{g_p(v,v)}$$
\end{defn}

\begin{defn}{Piecewise Regular Paths}{}\\
Let $(M,g)$ be a Riemannian manifold. A piecewise regular path in $M$ consists of smooth functions $\gamma_i:[a_i,b_i]\to M$ for $1\leq i\leq k$ such that the following are true. 
\begin{itemize}
\item For $1\leq i\leq k-1$, we have $\gamma_i(b_i)=\gamma_{i+1}(a_i)$. 
\item For $1\leq i\leq k$, we have $\abs{\gamma_i'(t)}\neq 0$ for all $t\in[a_i,b_i]$. 
\end{itemize}
\end{defn}

\begin{defn}{Length of a Curve}{}\\
Let $(M,g)$ be a Riemannian manifold. Let $\gamma:[a,b]\to M$ be a piecewise regular path in $M$. Define the length of $\gamma$ to be $$L(\gamma)=\int_a^b\abs{\gamma'(t)}_g\;dt$$
\end{defn}

\begin{defn}{Riemannian Distance Function}{}\\
Let $(M,g)$ be a Riemannian manifold. Define the Riemannian distance function of $M$ to be the function $d_g:M\times M\to\R$ given by $$d_g(p,q)=\inf\{L(\gamma)\;|\;\gamma:I\to M\text{ is a piecewise regular path }\}$$
\end{defn}

\begin{prp}{}{}\\
Let $(M,g)$ be a Riemannian manifold. Then the following are true. 
\begin{itemize}
\item $d_g$ is a metric on $M$. 
\item The topology of $M$ is given by the metric topology induced by $d_g$. 
\end{itemize}
\end{prp}

\begin{eg}{}{}\\
For a moment, take it for granted that the short distance between two points on $S^n$ is just a segment of a great circle. Then for $p,q\in S^n$, we have $$d(p,q)=\int_0^1\abs{\gamma'(t)}_g\;dt$$ where $\gamma:[0,1]\to S^n$ is the path given as follows. Let $\theta\in[0,\pi]$ be the unique angle such that $\cos(\theta)=\langle p,q\rangle_{\R^{n+1}}$. Suppose that $p\neq\pm q$. Then $\gamma$ is given by $$\gamma(t)=\frac{\sin((1-t)\theta)}{\sin(\theta)}p+\frac{\sin(t\theta)}{\sin(\theta)}q$$ Then we have $$\gamma'(t)=\theta\left(-\frac{\cos((1-t)\theta)}{\sin(\theta)}p+\frac{\cos(t\theta)}{\sin(\theta)}q\right)$$ Then we have $\abs{\gamma'(t)}=\theta$ and so we have $$d(p,q)=\theta$$ This is precisely the spherical metric introduced in Geometry. 
\end{eg}

\pagebreak
\section{Introduction to Connections}
\subsection{Connections on Vector Bundles}
Recall that for a smooth vector bundle $p:E\to M$, we denote the space of smooth sections on $E$ by $\Gamma(E)$. Moreover, $\mathfrak{X}(M)$ is the space of smooth sections on the tangent bundle $TM$. 

\begin{defn}{Connections}{}\\
Let $M$ be a smooth manifold. Let $p:E\to M$ be a smooth vector bundle. A connection on $p$ is a map $$\nabla:\mathfrak{X}(M)\times\Gamma^\infty(E)\to\Gamma^\infty(E)$$ where we denote $\nabla(V,T)$ by $\nabla_V(T)$, such that the following are true. 
\begin{itemize}
\item $C^\infty(M)$-linearity in first variable: For each $T\in\Gamma^\infty(E)$, the map $V\mapsto\nabla_V(T)$ is $C^\infty(M)$-linear. This means that $$\nabla_{fV+hW}(T)=f\nabla_V(T)+g\nabla_W(T)$$ for $V,W\in\mathfrak{X}(M)$, $f,g\in C^\infty(M)$. 
\item $\R$-linearity in second variable: For each $V\in\mathfrak{X}(M)$, the map $T\mapsto\nabla_V(T)$ is $\R$-linear. This means that $$\nabla_V(\lambda T+\mu S)=\lambda\nabla_V(T)+\mu\nabla_V(S)$$
\item Product rule: The map $\nabla$ satisfies the following product rule: $$\nabla_V(fT)=V(f)\cdot T+f\nabla_V(T)$$
\end{itemize}
\end{defn}

\begin{prp}{}{}\\
Let $M$ be a smooth manifold. Let $p:E\to M$ be a smooth vector bundle. Then $E$ admits a connection. 
\end{prp}

\begin{defn}{Set of Connections}{}\\
Let $M$ be a smooth manifold. Let $p:E\to M$ be a smooth vector bundle. Define the set of connections of $E$ to be $$\mA(E)=\{\nabla:\mathfrak{X}(M)\times\Gamma^\infty(E)\to\Gamma^\infty(E)\;|\;\nabla\text{ is a connection }\}$$
\end{defn}

\begin{prp}{}{}\\
Let $M$ be a smooth manifold. Let $p:E\to M$ be a smooth vector bundle. A connection $\nabla$ on $E$ is equivalently an $\R$-linear map $$\nabla:\Gamma^\infty(E)\to\Gamma^\infty(T^\ast M\otimes E)=\Omega^1(M)\otimes_{\mC^\infty(M)}\Gamma^\infty(E)$$ such that $$\nabla(f\cdot s)=df\otimes s+f\cdot\nabla s$$ for all $f\in\mC^\infty(M)$ and $s\in\Gamma^\infty(E)$. 
\end{prp}

\begin{defn}{The Connection Forms}{}\\
Let $M$ be a smooth manifold. Let $p:E\to M$ be a smooth vector bundle. Let $\nabla$ be a connection on $E$. Let $(U,e_1,\dots,e_r)$ be a local frame of $E$. Define the connection $1$-forms $\omega_j^i$ of $\nabla$ at the frame by $$\nabla_X(e_j)=\sum_{i=1}^r\omega_j^i(X)e_i$$ for all $X\in\mathfrak{X}(U)$. 
\end{defn}

\begin{prp}{}{}\\
Let $M$ be a smooth manifold. Let $p:E\to M$ be a smooth vector bundle. Let $\nabla$ be a connection on $E$. Let $(U,e_1,\dots,e_r)$ and $(U,\overline{e_1},\dots,\overline{e_r})$ be local frames of $E$. Suppose that $\omega_j^i$ and $\overline{\omega}_j^i$ are the corresponding connection forms. Suppose that $A:U\to\text{GL}(r,\R)$ is a transition function such that $\overline{e}=Ae$. Then we have $$\overline{\omega}_j^i=A^{-1}\omega_j^i A+A^{-1}dA$$
\end{prp}

\subsection{Connections of Tensor Fields}
\begin{defn}{The Induced Connection on the Dual Bundle}{}\\
Let $M$ be a smooth manifold. Let $p:E\to M$ be a smooth vector bundle. Let $\nabla$ be a connection on $M$. Define the induced connection on $E^\ast$ to be the connection $\nabla:\mathfrak{X}(M)\times\Omega^1(M)\to\Omega^1(M)$ by $$\nabla_X\xi(s)=X(\xi(s))-\xi(\nabla_Xs)$$
\end{defn}

\begin{defn}{The Induced Connection on Tensor Fields}{}\\
Let $M$ be a smooth manifold. Let $p:E\to M$ be a smooth vector bundle. Let $\nabla$ be a connection on $M$. Define the induced connection of $\nabla$ to be the connection $\nabla:\mathfrak{X}(M)\times\mC^\infty(\mT_q^p(E))\to\mC^\infty(\mT_q^p(E))$ given by 
\begin{align*}
\nabla_XF(\omega_1,\dots,\omega_p,Y_1,\dots,Y_q)=&X(F(\omega_1,\dots,\omega_p,Y_1,\dots,Y_q))\\
&-\sum_{i=1}^pF(\omega_1,\dots,\nabla_X\omega_i,\dots,\omega_p,Y_1,\dots,Y_q)\\
&-\sum_{j=1}^qF(\omega_1,\dots,\omega_p,Y_1,\dots,\nabla_XY_i,\dots,Y_q)
\end{align*}
\end{defn}

\begin{defn}{The Total Covariant Derivative}{}\\
Let $M$ be a smooth manifold. Let $p:E\to M$ be a smooth vector bundle. Let $\nabla$ be a connection on $M$. Let $F$ be a tensor field of type $(p,q)$. Define the total covariant derivative of $F$ to be $\nabla F:X\mapsto\nabla_XF$. 
\end{defn}

\subsection{The Curvature Tensor}
\begin{defn}{The Curvature Tensor}{}\\
Let $M$ be a smooth manifold. Let $p:E\to M$ be a smooth vector bundle. Let $\nabla$ be a connection on $p$. Define the curvature of $\nabla$ to be the smooth section $R(\nabla)\in\mC^\infty(\text{End}(E)\otimes\Lambda^2T^\ast M)$ given by $$R(\nabla)(X,Y)(s)=[\nabla_X,\nabla_Y](s)-\nabla_{[X,Y]}(s)$$ for $X,Y\in\mathfrak{X}(M)$ and $s\in\mC^\infty(E)$. 
\end{defn}

Notice that the definition makes sense: Appealing to the tensor characterization lemma, a section of $\mC^\infty(\text{End}(E)\otimes\Lambda^2T^\ast M)$ corresponds to a homomorphism $\Lambda^2TM\to\mC^\infty(\text{End}(E))$. Applying the lemma to $\mC^\infty(\text{End}(E))=\mC^\infty(E^\ast\otimes E)$ again, we see that the curvature tensor corresponds to a map $$\Lambda^2TM\to\Hom_{\mC^\infty(M)}(\mC^\infty(E),\mC^\infty(E))$$

\begin{defn}{The Curvature Form}{}\\
Let $M$ be a smooth manifold. Let $p:E\to M$ be a smooth vector bundle. Let $\nabla$ be a connection on $E$. Let $U\subseteq M$ be an open subset. Let $e_1,\dots,e_r$ be a local frame of $E$ on $U$. Define the curvature forms of $\nabla$ on $U$ to be the unique two forms $\Omega_j^i\in\mC^\infty(U,E)$ such that $$R(\nabla)(X,Y)(e_j)=\sum_{i=1}^r\Omega_j^i(X,Y)e_j$$ for all $X,Y\in\mathfrak{U}$. 
\end{defn}

\begin{thm}{The Second Structural Equation}{}\\
Let $M$ be a smooth manifold. Let $p:E\to M$ be a smooth vector bundle. Let $\nabla$ be a connection on $E$. Let $U\subseteq M$ be an open subset. Let $e_1,\dots,e_r$ be a local frame of $E$ on $U$. Then we have $$\Omega_j^i=d\omega_j^i+\sum_{k=1}^r\omega_k^i\wedge\omega_j^k$$
\end{thm}

Another way to think about it: The matrix $\Omega=(\Omega_j^i)$ is an element of $\Omega^2(U,\mathfrak{gl}(n,\R))$ (indeed locally $\text{End}(E)$ is isomorphic to $\mathfrak{gl}(n,\R)$). Gluing along local patches give $\Omega=d\omega+\omega\wedge\omega$. One can prove that $R(X,Y)=\Omega(X,Y)$. Indeed, $\Omega$ comes from the coordinate description of $R$, so patching it up should recover $R$. 

\begin{prp}{}{}\\
Let $M$ be a smooth manifold. Let $p:E\to M$ be a smooth vector bundle. Let $\nabla$ be a connection on $E$. Let $U\subseteq M$ be an open subset. Let $e,\overline{e}$ be local frames of $E$ on $U$. Suppose that $g:U\to\text{GL}(r,\R)$ is a transition function such that $\overline{e}=ge$. Suppose that $\Omega_j^i$ and $\overline{\Omega}_j^i$ are the curvature forms of $\nabla$ with respect to $e$ and $\overline{e}$. Then we have $$\overline{\Omega}_j^i=g^{-1}\Omega g$$
\end{prp}

\subsection{Directional Derivatives of a Vector Bundle}
\begin{defn}{Covariant Derivatives Along a Curve}{}\\
Let $M$ be a smooth manifold. Let $p:E\to M$ be a smooth vector bundle. Let $\gamma:[0,1]\to M$ be a smooth curve. Let $\nabla$ be a connection on $E$. Define the covariant derivative $D_t:\mC^\infty(\gamma^\ast(E))\to\mC^\infty(\gamma^\ast(E))$ of $\gamma$ to be the pullback of the connection $\nabla$ on $\gamma$. 
\end{defn}

\begin{prp}{}{}\\
Let $M$ be a smooth manifold. Let $p:E\to M$ be a smooth vector bundle. Let $\gamma:[0,1]\to M$ be a smooth curve. Let $\nabla$ be a connection on $E$. Then the covariant derivative is given in local charts $(U,\phi=(x^1,\dots,x^n))$ for $\gamma(t_0)\in M$ by the formula $$(D_tV)_{\gamma(t_0)}=\sum_{i=1}^n\left(\frac{\partial a_i}{\partial x^i}\bigg{|}_{\gamma(t_0)}\frac{\partial}{\partial x^i}\bigg{|}_{\gamma(t_0)}+a_i(\gamma(t_0))\nabla\left(\gamma'(t_0),\frac{\partial}{\partial x^i}\bigg{|}_{\gamma(t_0)}\right)\right)$$ where $V$ is given locally on the same chart by $$V_{\gamma(t)}=\sum_{i=1}^na_i(\gamma(t))\frac{\partial}{\partial x^k}\bigg{|}_{\gamma(t)}$$
\end{prp}

\begin{defn}{Parallel Sections}{}\\
Let $M$ be a smooth manifold. Let $p:E\to B$ be a smooth vector bundle. Let $\gamma:[0,1]\to M$ be a smooth curve. Let $\nabla$ be a connection on $E$. Define the parallel sections of $\gamma$ along $\nabla$ to be $\ker(D_t)$. 
\end{defn}

In other words, a parallel section is a local section $s:\im(\gamma)\to E$ such that $D_t(s)=0$. 

\begin{prp}{}{}\\
Let $M$ be a smooth manifold. Let $p:E\to B$ be a smooth vector bundle. Let $\gamma:[0,1]\to M$ be a smooth curve. Let $\nabla$ be a connection on $E$. Let $X\in E_{\gamma(0)}$. Then there exists a unique parallel section $s:[0,1]\to E$ such that $s(0)=X$. 
\end{prp}

\begin{defn}{Parallel Transports}{}\\
Let $M$ be a smooth manifold. Let $p:E\to B$ be a smooth vector bundle. Let $\gamma:[0,1]\to M$ be a smooth curve. Let $\nabla$ be a connection on $E$. Define the parallel transport map $$P_\gamma:E_{\gamma(0)}\to E_{\gamma(1)}$$ by $X\mapsto s_X(1)$ where $s_X:[0,1]\to E$ is the unique parallel section such that $s_X(0)=X$. 
\end{defn}

\begin{lmm}{}{}\\
Let $M$ be a smooth manifold. Let $p:E\to B$ be a smooth vector bundle. Let $\gamma:[0,1]\to M$ be a smooth curve. Let $\nabla$ be a connection on $E$. Then $P_\gamma$ is a well defined linear map. 
\end{lmm}

\pagebreak
\section{Connections on the Tangent Bundle}
\subsection{Affine Connections}
\begin{defn}{Affine Connections}{} Let $M$ be a smooth manifold. An affine connection of $M$ is a connection $$\nabla:\mathfrak{X}(M)\times\mathfrak{X}(M)\to\mathfrak{X}(M)$$ on the tangent bundle $TM$. 
\end{defn}

The directional derivative is the canonical affine connection on $\R^n$. In fact, every smooth manifold has such a canonical connection that generalizes the directional derivative. \\

Since the tangent bundle has a canonical local frame for every local chart, affine connections admit canonical connection components. 

\begin{defn}{Connection Components}{}\\
Let $M$ be a smooth manifold. Let $\nabla$ be an affine connection. Define the connection components $\Gamma_{ij}^k$ of $\nabla$ locally at a chart $(U,x^1,\dots,x^n)$ of $M$ by $$\nabla_{\frac{\partial}{\partial x^i}}\frac{\partial}{\partial x^j}=\sum_{k=1}^n\Gamma_{ij}^k\frac{\partial}{\partial x^k}$$ for $1\leq i,j,k\leq n$. 
\end{defn}

\begin{prp}{}{}\\
Let $M$ be a smooth manifold. Let $\nabla$ be an affine connection. Let $X,Y\in\mathfrak{X}(M)$ be vector fields. Suppose that the components of $X$ and $Y$ are given by $X^i$ and $Y^i$ respectively. Then locally at a chart $(U,x^1,\dots,x^n)$ of $M$, we have $$\nabla_XY=\sum_{k=1}^n\left(X(Y^k)+\sum_{i,j=1}^nX^iY^j\Gamma_{ij}^k\right)\frac{\partial}{\partial x^k}$$
\end{prp}

\begin{defn}{The Torsion Tensor}{}\\
Let $M$ be a smooth manifold. Let $\nabla$ be an affine connection. Define the torsion of $\nabla$ to be the map $T(\nabla):\mathfrak{X}(M)\times\mathfrak{X}(M)\to\mathfrak{X}(M)$ given by $$T(\nabla)(X,Y)=\nabla_XY-\nabla_YX-[X,Y]$$
\end{defn}

\begin{prp}{}{}\\
Let $M$ be a smooth manifold. Let $\nabla$ be an affine connection. Then the components of the torsion tensor is given by $$T(\nabla)_{ij}^k=\Gamma_{ij}^k-\Gamma_{ji}^k$$
\end{prp}

\begin{defn}{The Torsion Form}{}\\
Let $M$ be a smooth manifold. Let $\nabla$ be an affine connection. Let $(U,e_1,\dots,e_n)$ be a smooth local frame of $E$. Define the torsion form $\tau^i$ of $\nabla$ at the frame by $$T(\nabla)(X,Y)=\sum_{i=1}^n\tau^i(X,Y)e_i$$
\end{defn}

\begin{thm}{The First Structural Equation}{}\\
Let $M$ be a smooth manifold. Let $\nabla$ be an affine connection. Let $(U,e_1,\dots,e_n)$ be a smooth local frame of $TM$. Then we have $$\tau^i=d\theta^i+\sum_{j=1}^n\omega_j^i\wedge\theta^j$$ where $(U,\theta^1,\dots,\theta^n)$ is the dual of the local frame in $T^\ast M$. 
\end{thm}

\begin{thm}{The First Bianchi Identity}{}\\
Let $M$ be a smooth manifold. Let $\nabla$ be an affine connection. Let $(U,e_1,\dots,e_n)$ be a smooth local frame of $TM$. Then we have $$d\tau^i=\sum_{j=1}^n\Omega_j^i\wedge\theta^j-\omega_j^i\wedge\tau^j$$ where $(U,\theta^1,\dots,\theta^n)$ is the induced local frame of $T^\ast M$. 
\end{thm}

\begin{thm}{The Second Bianchi Identity}{}\\
Let $M$ be a smooth manifold. Let $\nabla$ be an affine connection. Let $(U,e_1,\dots,e_n)$ be a smooth local frame of $TM$. Then we have $$d\Omega_j^i=\sum_{k=1}^n\Omega_k^i\wedge\omega_j^k-\omega_k^i\wedge\Omega_j^k$$ where $(U,\theta^1,\dots,\theta^n)$ is the induced local frame of $T^\ast M$. 
\end{thm}

\begin{defn}{The Einstein Tensor}{}\\
Let $M$ be a smooth manifold. Let $\nabla$ be an affine connection. Let $(U,e_1,\dots,e_n)$ be a smooth local frame of $TM$. Define the Einstein tensor to be the $(0,2)$-tensor field whose components are given by $$G_{ab}=R_{ab}-\frac{1}{2}g_{ab}R$$
\end{defn}

\begin{thm}{The Contracted Second Bianchi Identity}{}\\
Let $M$ be a smooth manifold. Let $\nabla$ be an affine connection. Let $(U,e_1,\dots,e_n)$ be a smooth local frame of $TM$. Then we have $$\sum_{a=1}^n\sum_{c=1}^ng^{ac}\nabla_{e_c}G_{ab}=0$$ for all $1\leq b\leq n$. 
\end{thm}

If $\nabla^{e_a}$ denotes raising the index ($\nabla^{e_a}=\sum_{c=1}^ng^{ac}\nabla_{e_c}$), then we recover the more common form of the Bianchi identity: $$\nabla^{e_a}G_{ab}=0$$

\subsection{Curvature and Torsion of Affine Connections}
Let $M$ be a smooth manifold. For an affine connection $\nabla$, the definition implies that $\nabla$ is a smooth section of $$\text{End}(TM)\otimes\Lambda^2 T^\ast M\cong TM\otimes T^\ast M\otimes\Lambda^2 T^\ast M$$ and this is exactly a map $TM\times TM\times TM\to TM$ where we think of the first two inputs as antisymmetric. 

\begin{defn}{Curvature Components}{}\\
Let $M$ be a smooth manifold. Let $\nabla$ be an affine connection on $M$. Define the curvature components $R_{kij}^l$ of $\nabla$ locally at a chart $(U,x^1,\dots,x^n)$ of $M$ by $$R(\nabla)\left(\frac{\partial}{\partial x^i},\frac{\partial}{\partial x^j}\right)\frac{\partial}{\partial x^k}=\sum_{l=1}^nR_{kij}^l\frac{\partial}{\partial x^l}$$
\end{defn}

\begin{prp}{}{}\\
Let $M$ be a smooth manifold. Let $\nabla$ be an affine connection on $M$. Then locally at a chart $(U,x^1,\dots,x^n)$ of $M$, we have $$R_{kij}^l=\frac{\partial\Gamma_{kj}^l}{\partial x^i}-\frac{\partial\Gamma_{ki}^l}{\partial x^j}+\sum_{m=1}^n\left(\Gamma_{mi}^l\Gamma_{kj}^m-\Gamma_{mj}^l\Gamma_{ki}^m\right)$$
\end{prp}

\begin{prp}{}{}\\
Let $(M,g)$ be a Riemannian manifold. Then $M$ is flat if and only if $R(\nabla^g)=0$. 
\end{prp}

Sometimes it is more convenient to lower the last index. 

\begin{defn}{Riemannian Curvature Tensor}{}\\
Let $M$ be a smooth manifold. Let $\nabla$ be an affine connection on $M$. Define the Riemannian curvature tensor of $\nabla$ by $$Rm(\nabla)=R(\nabla)^\flat$$
\end{defn}

\begin{lmm}{}{}\\
Let $M$ be a smooth manifold. Let $\nabla$ be an affine connection on $M$. Then the components of $Rm$ is given by $$Rm_{kijl}=\sum_{m=1}^ng_{lm}R_{kij}^m$$
\end{lmm}

\subsection{Metric Connections}
\begin{defn}{The Lie Bracket on Smooth Vector Fields}{} Let $M$ be a smooth manifold. Let $X,Y\in\mathfrak{X}(M)$ be smooth vector fields. Define the Lie bracket of $X$ and $Y$ to be the vector field $[X,Y]$ given by the formula $$[X,Y]_p(f)=X(Y_p(f))=Y(X_p(f))$$ for $p\in M$ and $f\in\mC_{M,p}^\infty$. 
\end{defn}

\begin{prp}{}{} Let $M$ be a smooth manifold. Let $X,Y\in\mathfrak{X}(M)$ be smooth vector fields. Let $(U,\phi)$ be a chart on $M$. Let $X$ and $Y$ be given locally on the chart by the formula $$X=\sum_{k=1}^na_k\frac{\partial}{\partial x^k}\;\;\;\;\text{ and }\;\;\;\;Y=\sum_{k=1}^nb_k\frac{\partial}{\partial x^k}$$ for $a_k,b_k:U\to\R$ smooth functions. Then the Lie bracket of $X$ and $Y$ is given locally on the chart by the formula $$[X,Y]=\sum_{k=1}^n\left(X(b_k)-Y(a_k)\right)\frac{\partial}{\partial x^k}=\sum_{i=1}^n\left(\sum_{j=1}^n\left(a_j\frac{\partial b_i}{\partial x^i}-b_i\frac{\partial a_j}{\partial x^j}\right)\right)\frac{\partial}{\partial x^i}$$
\end{prp}

\begin{defn}{Metric Connections}{} Let $M$ be a smooth manifold. Let $\nabla$ be an affine connection. We say that $\nabla$ is a metric connection if the $$\nabla(X,g(Y,Z))=g(\nabla(X,Y),Z)+g(Y,\nabla(X,Z))$$ for all $X,Y,Z\in\mathfrak{X}(M)$. 
\end{defn}

\subsection{The Levi-Civita Connection}
\begin{defn}{Levi-Civita Connections}{}\\
Let $(M,g)$ be a Riemannian manifold. Let $\nabla^g:\mathfrak{X}(M)\times\mathfrak{X}(M)\to\mathfrak{X}(M)$ be an affine connection on $M$. We say that $\nabla^g$ is a Levi-Civita connection if the following are true. 
\begin{itemize}
\item $\nabla^g$ is a metric connection. 
\item For any $X,Y\in\mathfrak{X}(M)$, we have $$\nabla^g(X,Y)-\nabla^g(Y,X)=[X,Y]$$
\end{itemize}
\end{defn}

\begin{thm}{Existence and Uniqueness of Levi-Civita Connections}{} Let $(M,g)$ be a Riemannian manifold. Then $M$ has a unique Levi-Civita connection. 
\end{thm}

\begin{defn}{Christoffel Symbols}{}\\
Let $(M,g)$ be a Riemannian manifold. Define the Christoffel symbols of $\nabla$ to be the connection coefficients of the Levi-Civita connection. 
\end{defn}

\begin{prp}{}{}\\
Let $(M,g)$ be a Riemannian manifold. Then locally at a chart $(U,x^1,\dots,x^n)$ of $M$ we have $$\Gamma_{ij}^k=\frac{1}{2}\sum_{l=1}^ng^{kl}\left(\frac{\partial g_{il}}{\partial x^j}+\frac{\partial g_{jl}}{\partial x^i}-\frac{\partial g_{ij}}{\partial x^l}\right)$$ for $1\leq i,j,k\leq n$. 
\end{prp}

\begin{thm}{The Decomposition Theorem}{}
\end{thm}

\subsection{Curvature and Torsion of the Levi-Civita Connection}
\begin{prp}{}{}\\
Let $(M,g)$ be a Riemannian manifold. Then the following are true regarding $Rm(\nabla^g)$. 
\begin{itemize}
\item $Rm(W,X,Y,Z)=-Rm(X,W,Y,Z)$. 
\item $Rm(W,X,Y,Z)=-Rm(W,X,Z,Y)$. 
\item $Rm(W,X,Y,Z)=Rm(Y,Z,W,X)$. 
\end{itemize}
\end{prp}

\begin{crl}{}{}\\
Let $(M,g)$ be a Riemannian manifold. Then the following are true regarding the components of $Rm(\nabla^g)$. 
\begin{itemize}
\item $Rm_{kijl}=-Rm_{ikjl}$. 
\item $Rm_{kijl}=-Rm_{kilj}$. 
\item $Rm_{kijl}=Rm_{jlki}$. 
\end{itemize}
\end{crl}

\begin{prp}{The Bianchi Identities}{}\\
Let $(M,g)$ be a Riemannian manifold. Then the following are true. 
\begin{itemize}
\item $Rm(W,X,Y,Z)+Rm(X,Y,W,Z)+Rm(Y,W,X,Z)=0$. 
\item $\nabla Rm(X,Y,Z,V,W)+\nabla Rm(X,Y,V,W,Z)+\nabla Rm(X,Y,W,Z,V)=0$. 
\end{itemize}
\end{prp}

\subsection{Ricci and Scalar Curvature}
\begin{defn}{Ricci Curvature}{}\\
Let $(M,g)$ be a Riemannian manifold. Let $p\in M$. Define the Ricci curvature of $M$ to be the map $\text{Ric}:TM\times TM\to\R$ given by $$\text{Ric}(X,Y)=\text{tr}(Z\mapsto R(\nabla^g)(Z,X)(Y))$$
\end{defn}

\begin{defn}{Ricci Components}{}\\
Let $(M,g)$ be a Riemannian manifold. Define the Ricci components $R_{ij}$ locally at a chart $(U,x^1,\dots,x^n)$ of $M$ by $$R_{ij}=\text{Ric}\left(\frac{\partial}{\partial x^i},\frac{\partial}{\partial x^j}\right)$$
\end{defn}

\begin{prp}{}{}\\
Let $(M,g)$ be a Riemannian manifold. Then locally at a chart $(U,x^1,\dots,x^n)$ of $M$, we have $$R_{ij}=\sum_{k=1}^nR_{kij}^k$$
\end{prp}

\begin{defn}{Scalar Curvature}{}\\
Let $(M,g)$ be a Riemannian manifold. Define the scalar curvature of $M$ by $$R=\sum_{i,j=1}^ng^{ij}R_{ij}$$
\end{defn}

\pagebreak
\section{Geometry on Riemannian Manifolds}
\subsection{Geodesics on Riemannian Manifolds}
Let $M$ be a smooth manifold. Let $\gamma:I\to M$ be a smooth curve on $M$. Then we can compute its differential to obtain a tangent vector $$\gamma'(t)\in T_{\gamma(t)}M$$ for each $t\in I$. This defines a vector field in $\mathfrak{X}(\gamma)$. 

\begin{defn}{Geodesics}{} Let $M$ be a smooth manifold. Let $\nabla$ be an affine connection on $M$. A geodesic on $M$ is a curve $\gamma:I\to M$ such that $$D_t(\gamma'(t))=0$$ where $D_t$ is the associated covariant derivative of $\nabla$ and $\gamma$. 
\end{defn}

We have seen geodesics for metric spaces, but these are slightly different. We require geodesics on manifolds to only minimize the distance locally. 

\subsection{Minimizing Curves}
\begin{defn}{Minimizing Curves}{}\\
Let $(M,g)$ be a Riemannian manifold. Let $\gamma:[a,b]\to M$ be a smooth curve in $M$. We say that $\gamma$ is minimizing if $$L(\gamma)=d_g(\gamma(a),\gamma(b))$$
\end{defn}

\begin{defn}{Locally Minimizing Curves}{}\\
Let $(M,g)$ be a Riemannian manifold. Let $\gamma:[a,b]\to M$ be a smooth curve in $M$. We say that $\gamma$ is locally minimizing if for all $t\in[a,b]$, there exists a neighbourhood $[c,d]\subseteq[a,b]$ of $t$ such that $\gamma|_{[c,d]}$ is a minimizing curve. 
\end{defn}

\begin{prp}{}{}\\
Let $(M,g)$ be a Riemannian manifold. Let $\gamma:[a,b]\to M$ be a smooth curve in $M$. Then the following are true. 
\begin{itemize}
\item If $\gamma$ is a minimizing curve, then $\gamma$ is a geodesic. 
\item If $\gamma$ is a geodesic, then $\gamma$ is a locally minimizing curve. 
\end{itemize}
\end{prp}

\subsection{Geodesically Complete Riemannian Manifolds}
\begin{defn}{Geodesically Complete}{}
\end{defn}

\begin{prp}{}{}\\
Geodesically complete iff exp map well defined
\end{prp}

\begin{thm}{Hopf-Rinow Theorem}{}\\
Let $(M,g)$ be a connected Riemannian manifold. Then the following are equivalent. 
\begin{itemize}
\item $M$ is geodeically complete. 
\item $M$ is complete as a metric space. 
\item Every closed and bounded set in $M$ is compact. 
\end{itemize}
\end{thm}

\begin{prp}{}{}\\
Let $(M,g)$ be a connected Riemannian manifold. Let $p,q\in M$. Then there exists a minimizing geodesic from $p$ to $q$. 
\end{prp}

\pagebreak
\section{Gauss-Bonnet Theorem}
\subsection{The Second Fundamental Form}

\subsection{Gauss-Bonnet Theorem}
\begin{thm}{The Gauss-Bonnet Formula}{} Let $(M,g)$ be an oriented smooth $2$-manifold. Let $\gamma$ be a positively oriented curved polygon in $M$ and let $\Omega$ be its interior. Then $$\int_\Omega K\,dA+\int_\gamma\kappa_N\,ds+\sum_{i=1}^k\varepsilon_i=2\pi$$ where 
\begin{itemize}
\item $K$ is the Gaussian curvature of $g$
\item $dA$ is the Riemannian volume form
\item $\varepsilon_i$ are the exterior angles of $\gamma$
\item The second integral is taken with respect to arc length
\end{itemize}	
\end{thm}

\begin{thm}{Gauss-Bonnet Theorem}{} Let $(M,g)$ be an smooth compact $2$-dimensional Riemannian manifold. Let $K$ be the Gaussian curvature of $M$ and let $k_g$ be the geodesic curvature of $\partial M$. Then $$\int_MK\,dA+\int_{\partial M}k_g\,ds=2\pi\chi(M)$$
\end{thm}

\begin{crl}{}{} Let $(M,g)$ be an smooth compact $2$-dimensional Riemannian manifold without boundary. Let $K$ be the Gaussian curvature of $M$. Then $$\int_MK\,dA=2\pi\chi(M)$$
\end{crl}

\pagebreak
\section{Differential Operators}
\subsection{The Riemannian Volume Form and Density}
\begin{defn}{The Riemannian Volume Form}{}\\
Let $(M,g)$ be an orientable Riemannian manifold. Define the volume form $dV$ of $M$ to be given locally at $p\in M$ on the chart $(U,x^1,\dots,x^n)$ by $$(dV)_p=\sqrt{\det(g_{ij}(p))}(dx^1)_p\wedge\cdots\wedge(dx^n)_p$$ where $g$ is given locally at the chart by $g=\sum_{1\leq i,j\leq n}g_{ij}(p)dx^i\otimes dx^j$. 
\end{defn}

Let $(M,g)$ be a compact orientable Riemannian manifold. Let $f:M\to\R$ be a compactly supported smooth function. Then we may define the integral of $f$ over $M$ to be the value $\int_MfdV$. 

\begin{defn}{The Induced Volume Form at the Boundary}{}\\
Let $(M,g)$ be an orientable Riemannian manifold with boundary. Define the induced volume form on $\partial M$ to be $$dS=\iota^\ast dV$$
\end{defn}

\begin{lmm}{}{}\\
Let $(M,g)$ be an orientable Riemannian manifold with boundary. Let $(U,\phi)$ be a boundary chart of $M$. Then locally on $U$, the induced volume form is given by $$dS=\sqrt{\det(g_{ij}|_{\partial M})}dx^1\wedge\cdots\wedge dx^n$$
\end{lmm}

\begin{defn}{Riemannian Density}{}\\
Let $(M,g)$ be a Riemannian manifold. Define the Riemannian density $\mu$ to be the density given at a local chart $(U,x^1,\dots,x^n)$ by $$\mu=\sqrt{\det(g_{ij})}\abs{dx^1\wedge\cdots\wedge dx^n}$$
\end{defn}

\begin{prp}{}{}\\
If $M$ is orientable, then volume form of $M$ is just the Riemannian density. 
\end{prp}

\begin{defn}{Integrating Functions}{}\\
Let $(M,g)$ be a Riemannian manifold. Let $\mu$ be the Riemannian density of $M$. Let $f\in\mC^\infty(M)$ such that $f\mu$ is a compactly supported density. Define the integral of $f$ on $M$ by $$\int_Mf\mu$$
\end{defn}

\subsection{Differential Operators}
\begin{defn}{The Gradient of a Function}{}\\
Let $(M,g)$ be a Riemannian manifold. Let $f:M\to\R$ be a smooth function. Define the gradient of $f$ to be $$\text{grad}(f)=(df)^\#$$
\end{defn}

By definition, the gradient of $f$ is the unique section of $TM$ such that $$(df)_p(w)=g_p(\text{grad}(f)_p,w)$$ for all $p\in M$ and $w\in T_pM$. 

\begin{prp}{}{}\\
Let $(M,g)$ be a Riemannian manifold. Let $(U,x^1,\dots,x^n)$ be a chart in $M$. Let $f:M\to\R$ be a smooth function. Let $g$ be given locally at the chart by $$g_p=\sum_{1\leq i,j\leq n}g_{i,j}(p)(dx^i)_p\otimes(dx^j)_p$$ Let $(g^{i,j}(p))=(g_{i,j}(p))^{-1}$ be the inverse of the matrix. Then locally at $U$, the gradient of $f$ at $p$ is given by $$\text{grad}(f)(p)=\sum_{1\leq i,j\leq n}\frac{\partial f}{\partial x^i}\bigg{|}_pg^{i,j}(p)\frac{\partial}{\partial x^j}\bigg{|}_p$$
\end{prp}

\begin{eg}{}{}\\
Let $f:\R^n\to\R$ be a smooth function. Then the gradient of $f$ at $p\in\R^n$ is given by $$\text{grad}(f)(p)=\sum_{i=1}^n\frac{\partial f}{\partial x_i}\bigg{|}_p\frac{\partial}{\partial x_i}\bigg{|}_p$$
\end{eg}

\begin{defn}{The Divergence of a Vector Field}{}\\
Let $(M,g)$ be a Riemannian manifold. Let $X:M\to TM$ be a smooth vector field. Define the divergence of $X$ to be the unique smooth function $\text{div}(X):M\to\R$ such that for each $p\in M$ and $(U,x^1,\dots,x^n)$ a chart at $p$, we have $$(d(X\lrcorner dV))_p=(\text{div}(X)(p))(dV)_p$$ where $dV$ is the local top form given by $(dV)_p=\sqrt{\det(g_{i,j}(p))}(dx^1)_p\wedge\cdots\wedge(dx^n)_p$ and $g$ is given locally at the chart by $g_p=\sum_{1\leq i,j\leq n}g_{i,j}(p)(dx^i)_p\otimes(dx^j)_p$. 
\end{defn}

\begin{prp}{}{}\\
Let $(M,g)$ be a Riemannian manifold. Let $(U,x^1,\dots,x^n)$ be a chart in $M$. Let $X:M\to TM$ be a smooth vector field. Suppose that $g$ and $X$ are given locally at the chart by $$g_p=\sum_{1\leq i,j\leq n}g_{i,j}(p)(dx^i)_p\otimes(dx^j)_p\;\;\;\text{ and }\;\;\;X(p)=\sum_{i=1}^na_i\frac{\partial}{\partial x^i}\bigg{|}_p$$ respectively. Then locally at $U$, the divergence of $X$ at $p\in U$ is given by $$\text{div}(X)(p)=\frac{1}{\sqrt{\det(g_{i,j}(p))}}\sum_{k=1}^n\frac{\partial(a_k\circ\sqrt{\det(g_{i,j})})}{\partial x^k}\bigg{|}_p$$
\end{prp}

\begin{eg}{}{}\\
Let $X:\R^n\to\R^n$ be a smooth vector field whose components are given by $X_i:\R^n\to\R$. Then the divergence of $X$ at $p\in\R^n$ is given by $$\text{div}(X)(p)=\sum_{i=1}^n\frac{\partial X_i}{\partial x_i}\bigg{|}_p$$
\end{eg}

\begin{defn}{The Laplacian of a Function}{}\\
Let $(M,g)$ be a Riemannian manifold. Let $f:M\to\R$ be a smooth function. Define the Laplacian of $f$ to be the function $\Delta f:M\to\R$ given by $$\Delta f=\text{div}(\text{grad}(f))$$
\end{defn}

\begin{prp}{}{}\\
Let $(M,g)$ be a Riemannian manifold. Let $f:M\to\R$ be a smooth function. Let $(U,x^1,\dots,x^n)$ be a chart in $M$. Let $g$ be given locally at the chart by $$g_p=\sum_{1\leq i,j\leq n}g_{i,j}(p)(dx^i)_p\otimes(dx^j)_p$$ Let $(g^{i,j}(p))=(g_{i,j}(p))^{-1}$ be the inverse of the matrix. Then locally at $U$, the laplcian of $f$ at $p$ is given by $$\Delta(f)(p)=\frac{1}{\sqrt{\det(g_{i,j}(p))}}\sum_{1\leq k,l\leq n}\frac{\partial(g^{k,l}\circ\sqrt{\det(g_{i,j})}\circ\frac{\partial f}{\partial x^l})}{\partial x^k}\bigg{|}_p$$
\end{prp}

\begin{eg}{}{}\\
Let $f:\R^n\to\R$ be a smooth function. Then the laplacian of $f$ at $p\in\R^n$ is given by $$\Delta(f)(p)=\sum_{i=1}^n\frac{\partial^2f}{\partial x_i^2}\bigg{|}_p$$
\end{eg}

\subsection{Curl and The Case of Dimension 3}
\begin{defn}{The $\beta$-Operator}{}\\
Let $(M,g)$ be an oriented Riemannian manifold of dimension $3$. Define the $\beta$-operator to be the map $\beta:TM\to\bigwedge_{i=1}^2(T^\ast M)$ by $$\beta(X)=X\lrcorner dV$$
\end{defn}

\begin{lmm}{}{}\\
Let $(M,g)$ be an oriented Riemannian manifold of dimension $3$. Then $\beta$ is a smooth bundle isomorphism. 
\end{lmm}

\begin{defn}{The Curl of a Vector Field}{}\\
Let $(M,g)$ be an oriented Riemannian manifold of dimension $3$. Let $X:M\to TM$ be a smooth vector field. Define the curl of $X$ to be the vector field $\text{curl}(X):M\to TM$ given by $$\text{curl}(X)=\beta^{-1}(d(X^\flat))$$
\end{defn}

\begin{prp}{}{}\\
Let $(M,g)$ be an oriented Riemannian manifold of dimension $3$. Then the following diagram is commutative: \\
\adjustbox{scale=1.0,center}{\begin{tikzcd}
	{\mC^\infty(M)} & {\mathfrak{X}(M)} & {\mathfrak{X}(M)} & {\mC^\infty(M)} \\
	{\Omega^0(M)} & {\Omega^1(M)} & {\Omega^2(M)} & {\Omega^3(M)}
	\arrow["{\text{grad}}", from=1-1, to=1-2]
	\arrow["{\text{id}}"', from=1-1, to=2-1]
	\arrow["{\text{curl}}", from=1-2, to=1-3]
	\arrow["\flat"', from=1-2, to=2-2]
	\arrow["{\text{div}}", from=1-3, to=1-4]
	\arrow["\beta", from=1-3, to=2-3]
	\arrow["\flat", from=1-4, to=2-4]
	\arrow["d"', from=2-1, to=2-2]
	\arrow["d"', from=2-2, to=2-3]
	\arrow["d"', from=2-3, to=2-4]
\end{tikzcd}}\\
\end{prp}

\subsection{Classical Vector Calculus}
Let $M$ be an oriented smooth manifold of dimension $n$. let $p\in\partial M$. Then $T_p(\partial M)$ is an $(n-1)$-dimensional vector subspace of $T_p(M)$. Recall that a boundary chart of $M$ at $p\in M$ is a chart $(U,\phi)$ such that the following are true. 
\begin{itemize}
\item $\phi(U)\subseteq\{(x_1,\dots,x_n)\in\R^n\;|\;x_n\geq 0\}$. 
\item $\phi^{-1}(\{(x_1,\dots,x_n)\in\R^n\;|\;x_n=0\})\subseteq\partial M$. 
\item $\phi(p)=0$. 
\end{itemize}

\begin{defn}{The Unit Normal Vector Field}{}\\
Let $(M,g)$ be an oriented Riemannian manifold with boundary. Define the outward point unit normal vector field to be the unique smooth section $\nu:\partial M\hookrightarrow M\to TM$ such that the following are true. 
\begin{itemize}
\item $g_p(\nu(p),w)=0$ for all $w\in T_pM$ and all $p\in M$. 
\item $g_p(\nu(p),\nu(p))=1$ for all $p\in M$. 
\item For any $p\in M$ and any boundary chart $(U,\phi)$, the $n$th coordinate of $(\phi_\ast)(\nu_p)$ is strictly negative. 
\end{itemize}
\end{defn}

\begin{lmm}{}{}\\
Let $(M,g)$ be an oriented Riemannian manifold of dimension $n$ with boundary. Let $(U,\phi)$ be the boundary chart of $M$. Then locally at $U\cap\partial M$, the unit normal vector field is given by $$\nu=\frac{1}{\sqrt{g^{nn}}}\sum_{j=1}^ng^{jn}\frac{\partial}{\partial x^j}$$
\end{lmm}

\begin{lmm}{}{}\\
Let $(M,g)$ be an orientable Riemannian manifold with boundary. Then we have $$dS=\iota^\ast(\nu^\flat\lrcorner dV)$$
\end{lmm}

\begin{thm}{The Gradient Theorem}{}\\
Let $(M,g)$ be a Riemannian manifold. Let $\gamma:[0,1]\to M$ be a smooth curve. Let $f\in\mC^\infty(M)$ be a smooth function. Then we have $$\int_\gamma\;df=f(\gamma(1))-f(\gamma(0))$$
\end{thm}

\begin{thm}{The Divergence Theorem}{}\\
Let $(M,g)$ be a compact oriented Riemannian manifold with boundary. Let $\iota:\partial M\hookrightarrow M$ be the inclusion map. Let $X\in\mathfrak{X}(M)$. Then we have $$\int_M\text{div}(X)\;dV_g=\int_{\partial M}g(X,\nu)\;dS_g$$
\end{thm}

\pagebreak
\section{The Hodge Star Operator}
\subsection{The Hodge Star Operator}
\begin{defn}{The Hodge Star Operator}{}\\
Let $(M,g)$ be an orientable Riemannian manifold. Let $n=\dim(M)$. 
\begin{itemize}
\item Let $p\in M$. Define $\ast_p:\Lambda^kT_p^\ast(M)\to\Lambda^{n-k}T_p^\ast(M)$ to be the unique $\C$-linear isomorphism such that $$\alpha\wedge(\ast_p\beta)=g_p(\alpha,\beta)(dV)_p$$ where $\alpha,\beta\in\Lambda^kT_p^\ast(M)$ and $\text{Vol}$ is the volume form of $M$ associated to $g$, and $\langle-,-\rangle_p$ is the induced metric on $\Lambda^k(T_p^\ast(M))$. 
\item Define the Hodge star operator of $M$ to be the map $$\ast\in\Gamma(M,\Hom(\Omega^k(M),\Omega^{n-k}(M)))$$ given by $p\mapsto\ast_p$. 
\end{itemize}
\end{defn}

\begin{lmm}{}{}\\
Let $(M,g)$ be an orientable Riemannian manifold. Then the following are true regrading the Hodge star operator. 
\begin{itemize}
\item $\ast f=fdV$ for any smooth function $f\in\mC^\infty(M)$. 
\item $\ast\ast\omega=(-1)^{k(n-k)}\omega$ for any smooth $k$-form $\omega\in\Omega^k(M)$. 
\end{itemize}
\end{lmm}

\begin{lmm}{}{}\\
Let $(M,g)$ be an orientable Riemannian manifold. Let $X:M\to TM$ be a smooth vector field. Then the following are true. 
\begin{itemize}
\item $X\lrcorner dV=\ast(X^\flat)$. 
\item $\text{div}(X)=(\ast\circ d\circ\ast)(X^\flat)$
\end{itemize}
\end{lmm}

\begin{lmm}{}{}\\
Let $(M,g)$ be an orientable Riemannian manifold of dimension $3$. Let $X:M\to TM$ be a smooth vector field. Then we have $$\text{curl}(X)=(\ast\circ d(X^\flat))^\#$$
\end{lmm}

\subsection{The $L^2$-Norm}
\begin{defn}{The $L^2$-Norm}{}\\
Let $(M,g)$ be an orientable Riemannian manifold. Define the $L^2$-norm on $M$ to be the map $\langle -,-\rangle_{L^2}:\Omega^k(M)\times\Omega^k(M)\to\R$ by the formula $$\langle\alpha,\beta\rangle_{L^2}=\int_M\langle\alpha,\beta\rangle_gdV$$ where $\langle-,-\rangle_g$ is the induce inner product on $k$-forms. 
\end{defn}

\begin{lmm}{}{}\\
Let $(M,g)$ be an orientable Riemannian manifold. Then $\langle-,-\rangle_{L^2}$ defines an inner product on the compactly supported smooth $k$-forms on $M$ for any $k\in\N$. 
\end{lmm}

\begin{lmm}{}{}\\
Let $(M,g)$ be an orientable Riemannian manifold. Let $\alpha,\beta\in\Omega^k(M)$ be compactly supported smooth $k$-forms. Then we have $$\langle\alpha,\beta\rangle_{L^2}=\int_M\alpha\wedge\ast\beta$$
\end{lmm}

\subsection{The Adjoint Differential Operator}
\begin{defn}{The Adjoint Differential Operator}{}\\
Let $(M,g)$ be an orientable Riemannian manifold. Define the operator $d^\ast:\Omega^k(M)\to\Omega^{k-1}(M)$ by $$d^\ast(\omega)=(-1)^{n(k+1)+1}(\ast\circ d\circ\ast)(\omega)$$ for $\omega\in\Omega^k(M)$. Also define $d^\ast(\omega)=0$ for $\omega\in\Omega^0(M)$ by convention. 
\end{defn}

\begin{lmm}{}{}\\
Let $(M,g)$ be an orientable Riemannian manifold. Then we have $d^\ast\circ d^\ast=0$. 
\end{lmm}

\begin{prp}{}{}\\
Let $(M,g)$ be a compact orientable Riemannian manifold. Then we have $$\langle d^\ast\omega,\eta\rangle_{L^2}=\langle\omega,d\eta\rangle_{L^2}$$ for any $\omega\in\Omega^{k+1}(M)$ and $\eta\in\Omega^k(M)$. 
\end{prp}

\begin{defn}{The Laplace-Beltrami Operator}{}\\
Let $M$ be an orientable Riemannian manifold. Define the Laplace-Beltrami operator on $M$ to be the map $\Delta:\Omega^k(M)\to\Omega^k(M)$ given by $$\Delta=d^\ast\circ d+d\circ d^\ast$$
\end{defn}

\begin{lmm}{}{}\\
Let $M$ be an orientable Riemannian manifold. Then the following are true regarding the Laplace-Beltrami operator. 
\begin{itemize}
\item $\ast\circ\Delta=\Delta\circ\ast$. 
\item $\langle\Delta\alpha,\beta\rangle_{L^2}=\langle\alpha,\Delta\beta\rangle_{L^2}$ for any compactly supported differential forms $\alpha,\beta\in\Omega^k(M)$. 
\end{itemize}
\end{lmm}

\subsection{The Hodge Theorem for Riemannian Manifolds}
\begin{defn}{Harmonic Forms}{}\\
Let $M$ be an orientable Riemannian manifold. Let $\alpha\in\Omega^k(M)$. We say that $\alpha$ is harmonic if $\alpha\in\ker(\Delta)$. 
\end{defn}

\begin{lmm}{}{}\\
Let $M$ be an orientable Riemannian manifold. Let $\alpha\in\Omega^k(M)$. Then $\alpha$ is harmonic if and only if $d(\alpha)=0$ and $d^\ast(\alpha)=0$. 
\end{lmm}

\begin{defn}{The Space of Harmonic Forms}{}\\
Let $M$ be an orientable Riemannian manifold. Define the space of harmonic $k$-forms to be $$\mH^k(M)=\ker(\Delta)$$
\end{defn}

\begin{thm}{Hodge Theorem}{}\\
Let $M$ be an compact orientable Riemannian manifold. Then the following are true. 
\begin{itemize}
\item $\mH^k$ is finite dimensional for all $k\in\N$. 
\item There is a decomposition $$\Omega^k(M)\cong\mH^k(M)\oplus\im(\Delta)$$
\end{itemize}
\end{thm}

\begin{crl}{}{}\\
Let $M$ be an orientable Riemannian manifold. Then the quotient map induces an isomorphism $$\mH^k(M)\cong H_\text{dR}^k(M)$$ for all $k\in\N$. 
\end{crl}

\pagebreak
\section{Holonomy}
\subsection{Holonomy Groups}
\begin{defn}{Holonomy Groups}{}\\
Let $(M,g)$ be a Riemaniann manifold. Let $p:E\to M$ be a smooth vector bundle. Let $\nabla$ be a connection on $E$. Let $x\in M$. Define the holonomy group of $E$ at $x$ with respect to $\nabla$ to be $$\text{Hol}_x(\nabla)=\{P_\gamma\;|\;\gamma(0)=\gamma(1)=x\}\subseteq\text{GL}(E_x)$$
\end{defn}

\begin{lmm}{}{}\\
Let $(M,g)$ be a Riemaniann manifold. Let $p:E\to M$ be a smooth vector bundle. Let $\nabla$ be a connection on $E$. Let $x\in M$. Then $\text{Hol}_x(\nabla)$ is a Lie subgroup of $\text{GL}(E_x)$. 
\end{lmm}

\begin{prp}{}{}\\
Let $(M,g)$ be a Riemaniann manifold. Let $p:E\to M$ be a smooth vector bundle. Let $\nabla$ be a connection on $E$. Then the following are true. 
\begin{itemize}
\item Let $\gamma:[0,1]\to M$ be a path. Then we have $$\text{Hol}_{\gamma(1)}(\nabla)=P_\gamma\text{Hol}_{\gamma(0)}(\nabla)P_\gamma^{-1}$$
\item If $M$ is simply connected, then $\text{Hol}_x(\nabla)$ is connected for all $x\in M$. 
\end{itemize}
\end{prp}

So for simply connected spaces, the holonomy groups are independent of base point up to isomorphism. But the specific embedding into $\text{GL}(n,F)$ does depend on the base point. 

\begin{defn}{}{}\\
Let $M$ be a simply connected Riemannian manifold. Let $p:E\to M$ be a smooth vector bundle over $\R$. Let $\nabla$ be a connection on $E$. Define the holonomy group of $E$ with respect to $\nabla$ to be $$\text{Hol}(\nabla)=\text{Hol}_x(\nabla)$$ for any base point $x\in M$. 
\end{defn}

\begin{prp}{}{}\\
Let $(M,g)$ be a Riemaniann manifold. Let $p\in M$. Let $\phi:T_pM\to\R^n$ be an $\R$-linear isomorphism. Then we have $$\text{GL}(\phi)(\text{Hol}_p(\nabla^g))\leq O(n)$$
\end{prp}

\begin{prp}{}{}\\
Let $(M,g)$ and $(N,h)$ be Riemannian manifolds. Let $p\in M$ and $q\in N$. Then we have $$\text{Hol}_{(p,q)}(\nabla^{g\times h})=\text{Hol}_p(\nabla^g)\times\text{Hol}_p(\nabla^h)$$
\end{prp}

\begin{prp}{}{}\\
Let $(M,g)$ be a Riemannian manifold. Let $p\in M$. If $M$ is irreducible, then the representation $\text{Hol}_p(\nabla^g)\to\text{GL}(T_pM)$ is irreducible. 
\end{prp}

\begin{thm}{Berger}{}\\
Let $(M,g)$ be a Riemannian manifold of dimension $n$. Suppose that $M$ is simply connected, irreducible and not symmetric. Let $p\in M$. Then exactly one of the following cases hold. 
\begin{itemize}
\item $\text{Hol}_p(\nabla^g)\cong SO(n)$. 
\item $n=2m$ for $m\geq 2$ and $\text{Hol}_p(\nabla^g)\cong U(m)\leq SO(n)$. 
\item $n=2m$ for $m\geq 2$ and $\text{Hol}_p(\nabla^g)\cong SU(m)\leq SO(n)$. 
\item $n=4m$ for $m\geq 2$ and $\text{Hol}_p(\nabla^g)\cong SP(m)\leq SO(n)$. 
\item $n=4m$ for $m\geq 2$ and $\text{Hol}_p(\nabla^g)\cong SP(m)\times SP(1)\leq SO(n)$. 
\item $n=7$ and $\text{Hol}_p(\nabla^g)\cong G_2\leq SO(7)$. 
\item $n=8$ and $\text{Hol}_p(\nabla^g)\cong\text{Spin}(7)\leq SO(8)$. 
\end{itemize}
\end{thm}

\pagebreak
\section{Pseudo-Riemannian Manifolds}
\subsection{Basic Definitions}
In linear algebra, inner products are symmetric and positive-definite bilinear forms. This implies that inner products are non-degenerate. We may relax the positive-definiteness condition with non-degeneracy, and such bilinear forms are closely related to quadratic forms. Similarly, we may relax the Riemannian metric to such bilinear forms. 

\begin{defn}{Pseudo-Riemannian Metrics}{}\\
Let $M$ be a smooth manifold. A pseudo-Riemannian metric on $M$ is a smooth section $$g\in\mC^\infty((TM\otimes TM)^\ast)$$ such that $g_p:T_pM\times T_pM\to\R$ is a symmetric and non-degenerate bilinear form for all $p\in M$. 
\end{defn}

\begin{defn}{Pseudo-Riemannian Manifolds}{}\\
A pseudo-Riemannian manifold is a smooth manifold $M$ together with a pseudo-Riemannian metric $g$ on $M$. 
\end{defn}

\begin{prp}{}{}\\
Let $(M,g)$ be a pseudo-Riemannian manifold. Let $p\in M$. Let $(U,x^1,\dots,x^n)$ be a chart at $p$. Then $g_p$ as an element of $T_p(M)^\ast\otimes T_p(M)^\ast$ is given locally on the chart by $$g=\sum_{1\leq i,j\leq n}g_{ij}\;dx^i\otimes dx^j$$ where $g_{ij}:U\to\R$ are smooth functions such that $g_{ij}(p)=g_p\left(\frac{\partial}{\partial x^i}\bigg{|}_p,\frac{\partial}{\partial x^j}\bigg{|}_p\right)$. 
\end{prp}

\begin{defn}{Signature of the Metric}{}\\
Let $(M,g)$ be a pseudo-Riemannian manifold. Define the signature of $g$ to be the signature of $g_p$ as a quadratic form. 
\end{defn}

Not every smooth manifold admits a metric with signature $(p,q)$ whenever $q\neq 0$. 

\subsection{The Musical Isomorphisms}

\subsection{The Levi-Civita Connection}

\subsection{Curvature}

\subsection{Calculus on Manifolds}

\subsection{Hodge Star Operator}






\end{document}
