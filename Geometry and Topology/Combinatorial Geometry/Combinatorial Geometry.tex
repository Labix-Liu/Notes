\documentclass[a4paper]{article}

\input{C:/Users/liula/Desktop/Latex/Headers/Headers.tex}

\pagestyle{fancy}
\fancyhf{}
\rhead{Labix}
\lhead{Combinatorial Geometry}
\rfoot{\thepage}

\title{Combinatorial Geometry}

\author{Labix}

\date{\today}
\begin{document}
\maketitle

\pagebreak
\tableofcontents

\pagebreak
\section{Simplicies}
\subsection{Basic Definitions}
\begin{defn}{Affinely Independent}{}{}\\
Let $n,m\in\N$. We say that a set of points $\{v_0,\dots,v_m\}\subset\R^n$ is affinely independent if $v_1-v_0,\dots,v_n-v_0$ are linearly independent. 
\end{defn}

\begin{defn}{$n$-Simplexes}{}\\
Let $n\in\N$. Let $v_0,\dots,v_n\in\R^n$ be a set of affinely independent points. An $n$-simplex is the set of points $$\Delta^n=\left\{\sum_{k=0}^nt_kv_k\bigg{|}\sum_{k=0}^nt_k=1\text{ and }t_k\geq 0\text{ for all }k=0,\dots,n\right\}$$ We write $\Delta^n=[v_0,\dots,v_n]$ to indicate the spanning vectors. The standard $n$-simplex is just the $n$-simplex whose vertices are the standard basis vectors for $\R^{n+1}$. 
\end{defn}

Note that the vertices here are an ordered set so that an orientation is inherently defined. Realistically, the order of the vertices does not change the homology groups which we will define later. 

\begin{defn}{Properties of $n$-Simplexes}{} Let $\Delta^n=[v_0,\dots,v_n]$ be an $n$-simplex. 
\begin{itemize}
\item The $k$th face of $\Delta^n$ is a the $n-1$ simplex $\Delta_k^n=\Delta^n\cap\{x_k=0\}$. We use $[v_0,\dots,\hat{v}_k,\cdots,v_n]$ to indicate the $k$th $n-1$dimensional face
\item The boundary of $\Delta$, written $\partial\Delta^n$ is the union of all its proper faces
\item The interior is defined to be $(\Delta^n)^\circ=\Delta^n\setminus\partial\Delta^n$
\end{itemize}
\end{defn}

\begin{defn}{The Vertex Set of a Simplex}{}\\
Let $S=[v_0,\dots,v_n]\subseteq\R^n$ be a simplex. Define the vertex set of the simplex to be $$V(S)=\{v_0,\dots,v_n\}$$
\end{defn}

It is easy to see that any face of an $n$-simplex is an $n-1$ simplex in its own right. 

\begin{lmm}{}{} Any two $k$-simplexes where one in $\R^m$ and one in $\R^n$ are homeomorphic. 
\end{lmm}

\subsection{Barycentric Subdivision}
Barycentric subdivisions is the main ingredient for proving one of the major results of singular homology. It is defined first through $\Delta$-set, and then passed on to singular $n$-simplexes, which we will see in the next chapter. 

\begin{defn}{Barycenter}{} Let $v=[v_0,\dots,v_n]$ be an $n$-simplex. Define the barycenter of $v$ to be the point $$B(v)=\frac{1}{n+1}\sum_{i=0}^nv_i$$
\end{defn}

In fact, we can find the barycenter inductively. Knowing the barycenter $b_i$ on the $i$th face $[v_0,\dots,\hat{v}_i,\dots,v_n]$, let $l_i$ be the line connecting $b_i$ and $v_i$. Then $b$ is the intersection of all these lines $l_i$. 

\begin{defn}{Barycentric Cone}{} Let $v$ be an $n$-simplex. Let $w=[w_1,\dots,w_n]\subseteq v$ be an $(n-1)$-simplex in $v$. Define the barycentric cone of $w$ to be the $n$-simplex $$\mB(w)=[B(v),w_1,\dots,w_n]\subset v$$ 
\end{defn}

Intuitively, this means that given a face of $\Delta^n$ which is an $(n-1)$-simplex, its barycentric cone is the $n$-simplex constructed from the vertices of the face together with the barycenter. 

\begin{defn}{The Barycentric Map}{} Let $v$ be an $n$-simplex. Define the barycentric map of $v$ to be the map $$\mB:C_{n-1}(v)\to C_n(v)$$ defined on the basis by $\mB([w_1,\dots,w_n])=[B(v),w_1,\dots,w_n]$. 
\end{defn}

\begin{defn}{Barycentric Subdivision}{} Let $\Delta^n$ be the standard $n$-simplex. Define inductively the barycentric subdivision $$S^\Delta(\Delta^n)\in C_n(\Delta^n)$$ of $\Delta^n$ by the following recursive formula. 
\begin{itemize}
\item When $n=0$, $S^\Delta(\Delta^0)=\Delta^0$
\item When $n>0$, the barycentric subdivision of $\Delta^n$ is given by $$S^\Delta(\Delta^n)=\sum_{i=0}^n(-1)^i\mB S^\Delta(\partial_i\Delta^n)$$
\end{itemize}
\end{defn}

To elicit a few examples, consider the case of $n=1$. Then we have 
\begin{align*}
S(\Delta^1)&=\mB S(\partial\Delta^1)\\
&=\mB S(\partial_0\Delta^1)-\mB S(\partial_1\Delta^1)\\
&=\mB[v_1]-\mB[v_0]\\
&=[b,v_1]-[b,v_0]
\end{align*}
where $b=\frac{1}{2}(v_0+v_1)$. Intuitively, we are breaking up the $n$-simplex $\Delta^n$ into tiny pieces of $n$-simplexes using the center of mass of each subsimplex in $\Delta^n$. 

\begin{lmm}{}{} Let $\sigma=[w_1,\dots,w_n]\subseteq\Delta^n$ be an $(n-1)$-simplex. Then the following are true. 
\begin{itemize}
\item $\partial(\mB(\sigma))+\mB(\partial\sigma)=\sigma$ 
\item $\partial S(\Delta^n)=S(\partial\Delta^n)$
\end{itemize} 
\begin{proof}
We have that 
\begin{align*}
\partial\mB[w_1,\dots,w_n]&=\partial[b,w_1,\dots,w_n]\\
&=\sum_{i=0}^n(-1)^i\partial_i[b,w_1,\dots,w_n]\\
&=[w_1,\dots,w_n]-\mB(\partial[w_1,\dots,w_n])
\end{align*}
And thus the first identity is satisfied. \\
We prove the second item inductively. When $n=0$, we have $0$ on both sides. When $n>0$, we have
\begin{align*}
\partial S\Delta^n&=\partial\mB(S(\partial\Delta^n))\\
&=\text{id}(S\partial\Delta^n)-\mB(\partial(S\partial\Delta^n))\tag{First Identity}\\
&=S\partial\Delta^n-\mB(S\partial^2\Delta^n)\tag{Induction}\\
&=S\partial\Delta^n
\end{align*}
\end{proof}
\end{lmm}

\begin{lmm}{}{} Let $v=[v_0,\dots,v_n]$ be a simplex. Let $[w_0,\dots,w_n]$ be a component in the barycentric subdivision of $v$. Then we have $$\text{diam}([w_0,\dots,w_n])\leq\frac{n}{n+1}\text{diam}([v_0,\dots,v_n])$$ 
\begin{proof}
If $n=0$, then the claim is true since $[w_0]=[v_0]$ has diameter $0$. Assume that $n>0$. We first prove the following subclaim: For every $x\in[x_0,\dots,x_n]$ an $n$-simplex, its maximum distance to points in the simplex is attained at a vertex $x_i$. Indeed if $y\in[x_0,\dots,x_n]$ with $\|x-y\|$ maximal, then we can write $y=\sum_{i=1}^nt_ix_i$ with $\sum_{i=1}^nt_i=1$ and $t_i\geq 0$. Then we have that 
\begin{align*}
\|x-y\|&=\left\|x-\sum_{i=1}^nt_ix_i\right\|\\
&=\left\|\sum_{i=1}^nt_i(x-x_i)\right\|\\
&\leq\sum_{i=1}^nt_i\|x-x_i\|\\
&\leq\max\|x-x-i\|
\end{align*}
with equality if $y$ is one of the vertices with $\|x-x_i\|$ being maximal. By applying the above twice, we have that the diameter of $[w_0,\dots,w_n]$ is the length of the longest edge $[x,y]$ in $[w_0,\dots,w_n]$. Now there are two cases. \\

Case 1: None of $x,y$ is the barycenter $b$ of $[w_0,\dots,w_n]$. Then they must be the vertices of a simplex in the barycentric subdivision of one of the faces $[v_0,\dots,\hat{v_i},\dots,v_n]$. By induction, we have that 
\begin{align*}
\text{diam}([w_0,\dots,w_n])&=\|x-y\|\\
&\leq\frac{n-1}{n}\text{diam}([v_0,\dots,\hat{v_i},\dots,v_n])\\
&\leq\frac{n}{n+1}\text{diam}([v_0,\dots,v_n])
\end{align*}
and so we are done. \\

Case 2: Without loss of generality, $x=b$. \\
Then $y$ lies on some face of $[v_0,\dots,v_n]$ and the claim above implies that we can take $y=v_i$ for some vertex $v_i$ of that face. Let $b_i$ be the barycenter of $[v_0,\dots,\hat{v_i},\dots,v_n]$. Then we have that the barycenter of $[v_0,\dots,v_n]$ is $$b=\frac{1}{n+1}\sum_{j=1}^nv_j=\frac{1}{n+1}v_i+\frac{n}{n+1}b_i$$ We thus have that 
\begin{align*}
\text{diam}([w_0,\dots,w_n])&=\|v_i-b\|\\
&\leq\frac{n}{n+1}\|v_i-b_i\|\\
&\leq\frac{n}{n+1}\text{diam}([v_0,\dots,v_n])
\end{align*}
and so we conclude. 
\end{proof}
\end{lmm}

\pagebreak
\section{Simplicial and Delta Complexes}
\subsection{Simplicial Complexes}
\begin{defn}{Simplicial Complexes}{} Let $\mK$ be a set of simplexes. We say that $\mK$ is a simplicial complex if the following are true. 
\begin{itemize}
\item For any $S\in\mK$, every face $\partial_k S$ of $S$ lies in $\mK$. 
\item If $S_1$ and $S_2$ are two simplexes in $\mK$, then either $S_1\cap S_2$ is empty or $S_1\cap S_2$ is a common face of both $S_1$ and $S_2$. 
\end{itemize}
\end{defn}

\begin{defn}{Abstract Simplicial Complex}{}\\
Let $S$ be a set. We say that $S$ is a simplicial complex if for all $\sigma\in S$ and $\tau\subseteq\sigma$, we have $\tau\in S$. Elements of $S$ are called faces. 
\end{defn}

\begin{eg}{}{}\\
The set $S=\mP(\{1,2,3\})\cup\mP(\{1,3,4\})\cup\mP(\{2,4\})$ is an abstract simplicial complex. 
\end{eg}

\begin{defn}{Dimension}{}\\
Let $S$ be a simplicial complex. 
\begin{itemize}
\item Let $\sigma\in S$. Define the dimension of $\sigma$ to be $$\dim(\sigma)=\abs{\sigma}-1$$
\item Define the dimension of $S$ to be $$\dim(S)=\max\{\dim(\sigma)\;|\;\sigma\in S\}$$
\end{itemize}
\end{defn}

\begin{defn}{Vertex Set}{}\\
Let $S$ be a simplicial complex. Define the vertex set of $S$ to be $$V(S)=\bigcup_{\sigma\in S}\sigma$$
\end{defn}

Thus it is clear that $S\subseteq\mP(V(S))$. \\

We mostly consider the case where $V(S)$ is finite. In this case, we may replace the underlying set by $\{1,\dots,n\}$ by a suitable $n\in\N$. 


\begin{defn}{Associated Abstract Simplicial Complex}{}\\
Let $\mK$ be a simplicial complex. Define the associated abstract simplicial complex $A(\mK)\subseteq\mP(V(\mK))$ as follows: For each simplex $\Delta\in\mK$, we have $V(\Delta)\subseteq A(\mK)$. 
\end{defn}

\begin{lmm}{}{}\\
Let $\mK$ be a simplicial complex. Then $A(\mK)$ is an abstract simplicial complex. 
\end{lmm}

\subsection{Maps between Simplicial Complexes}
\begin{defn}{Simplicial Maps}{}\\
Let $S,T$ be simplicial complexes. Let $f:V(S)\to V(T)$ be a function. We say that $f$ is a simplicial map if for each $\sigma\in S$, we have $f(\sigma)\in T$. 
\end{defn}

\begin{lmm}{}{}\\
Let $S,T,U$ be simplicial complexes. Let $f:V(S)\to V(T)$ and $g:V(T)\to V(U)$ be simplicial maps. Then $g\circ f$ is a simplicial map. 
\end{lmm}

\begin{defn}{Isomorphism of Simplicial Complexes}{}\\
Let $S,T$ be simplicial complexes. Let $f:V(S)\to V(T)$ be a function. We say that $f$ is an isomorphism if there exists a simplicial map $g:V(T)\to V(S)$ such that $$g\circ f=\text{id}_{V(S)}\;\;\;\text{ and }\;\;\;f\circ g=\text{id}_{V(T)}$$
\end{defn}

\subsection{Barycentric Subdivision}
Recall that there is a notion of barycentric subdivision for simplicies. The definition easily extends to simplcial complexes, but not obvious for abstract simplicial complexes. 

\begin{defn}{Barycentric Subdivision}{}\\
Let $S$ be an abstract simplicial complex. Define the barycentric subdivision of $S$ to be the abstract simplicial complex $$\text{Bd}(S)=\bigcup_{i=0}^\infty\{\{v_0,\dots,v_k\}\subseteq\mP(S)\;|\;v_i\subset v_{i+1}\}$$ on $S$. 
\end{defn}

The idea is to add vertex for each simplex in the simplicial complex so that the underlying set of the barycentric subdivision is $S$. Since $S$ is a subset of $\mP(T)$, elements of $S$ are partially ordered by inclusion, each inclusion with the geometric meaning of faces of the simplex.

\subsection{Links and Stars}
\begin{defn}{The Link of a Face}{}\\
Let $S$ be a simplicial complex. Let $\sigma\in S$. Define the link of $\sigma$ to be $$\text{lk}(\sigma)=\{\tau\in S\;|\;\sigma\cup\tau\in S\text{ and }\sigma\cap\tau=\emptyset\}$$
\end{defn}

\begin{defn}{The Star of a Face}{}\\
Let $S$ be a simplicial complex. Let $\sigma\in S$. Define the star of $\sigma$ to be $$\text{st}(\sigma)=\{\tau\in S\;|\;\sigma\cup\tau\in S\}$$ Define the boundary of the star of $\sigma$ to be $$\partial\text{st}(\sigma)=\{\tau\in S\;|\;\sigma\cup\tau\in S\text{ and }\sigma\not\subseteq\tau\}$$
\end{defn}

\begin{lmm}{}{}\\
Let $S$ be a simplicial complex. Let $p\in V(S)$. Then we have $$\text{lk}(p)=\partial\text{st}(p)$$
\end{lmm}

\subsection{The Face Vector of a Simplicial Complex}
\begin{defn}{The Face Vector}{}\\
Let $S$ be an abstract simplicial complex on $\{1,\dots,n\}$. Define the face vector $(f_{-1},\dots,f_n)$ of $S$ by the formula $$f_i=\text{The number of }i\text{-dimensional faces of }S$$ for $0\leq i\leq n$ and by convention, $f_{-1}=1$. 
\end{defn}

\begin{defn}{The $h$-Vector}{}\\
Let $S$ be an abstract simplicial complex on $\{1,\dots,n\}$. Let $(f_0,\dots,f_n)$ be the face vector of $S$. Define the $h$-vector $(h_0,\dots,h_n)$ of $S$ by the formula $$h_i=\sum_{k=0}^i(-1)^{i-k}\binom{n+1-k}{i-k}f_{k-1}$$
\end{defn}

\begin{lmm}{}{}\\
Let $S$ be an abstract simplicial complex on $\{1,\dots,n\}$. Let $(f_0,\dots,f_n)$ be the face vector of $S$. Let $(h_0,\dots,h_n)$ be the $h$-vector of $S$. Then the following are true. 
\begin{itemize}
\item We have $$\sum_{k=0}^{n+1}f_{k-1}(t-1)^{d-k}=\sum_{j=0}^{n+1}h_jt^{n+1-j}$$ for variable $t$. 
\item For $0\leq i\leq n$, we have $$f_i=\sum_{k=0}^{i+1}\binom{n+1}{i+1-k}h_k$$
\end{itemize}
\end{lmm}

\subsection{Cubical Embeddings of Simplicial Complexes}
\begin{defn}{The Cubical Embedding}{}\\
Let $n\in\N$. Define the cubical embedding $i_c:\Delta^n\to[0,1]^{n+1}$ as follows. 
\begin{enumerate}
\item For $1\leq k\leq n$, the barycenter of the $k$-dimensional face $B([e_{i_1},\dots,e_{i_k}])$ is sent to $(u_1,\dots,u_{n+1})\in[0,1]^{n+1}$ where $u_j=0$ if $j\in\{i_1,\dots,i_k\}$ and $u_j=1$ otherwise. 
\item Extend this into a piecewise linear embedding $i_c:\Delta^n\to[0,1]^{n+1}$ as follows. For each simplex $[p_1,\dots,p_l]$ in the barycentric subdivision of $\Delta^n$, $[i_c(p_1),\dots,i_c(p_l)]$ lies on a face of $[0,1]^{n+1}$. Hence there exists an simplicial map sending $[p_1,\dots,p_l]$ to $[i_c(p_1),\dots,i_c(p_l)]$. 
\end{enumerate}
\end{defn}

\begin{lmm}{}{}\\
Let $n\in\N$. Then $\partial[0,1]^n$ is a simplicial complex. 
\end{lmm}

\begin{defn}{Cubical Embeddings of Abstract Simplicial Complexes}{}\\
Let $\mK$ be an abstract simplicial complex. Define the cubical embedding of $\mK$ to be the subspace $$\text{cc}(\mK)=i_c(\abs{\mK})$$
\end{defn}

\pagebreak
\section{Realizing Simplicial Complexes in Euclidean Space}
\subsection{Geometric Realization}
\begin{defn}{Geometric Realization with Respect to Function}{}\\
Let $S$ be an abstract simplicial complex. Let $n\in\N$. Let $\phi:V(S)\to\R^n$ be a function. Define the geometric realization of $S$ with respect to $\phi$ to be the space $$\abs{S}_\phi=\bigcup_{\sigma\in S}[\phi(s)\;|\;s\in\sigma]$$
\end{defn}

\begin{defn}{Affine Embedding}{}\\
Let $S$ be an abstract simplicial complex. Let $\phi:V(S)\to\R^n$ be a function. We say that $\abs{S}_\phi$ is an affine embedding if $\phi$ is injective. 
\end{defn}

\begin{prp}{}{}\\
Let $S$ be an abstract simplicial complex. Let $\phi:V(S)\to\R^n$ and $\psi:V(S)\to\R^m$ be affine embeddings. Then $\abs{S}_\phi$ and $\abs{S}_\psi$ are homeomorphic. 
\end{prp}

Since any two affine embeddings give homeomorphic geometric realizations, we often drop the underling injective function and assume that a choice has been chosen. 

\begin{defn}{Geometric Realization of a Simplicial Map}{}\\
Let $S,T$ be simplicial complexes. Let $\phi_1:V(S)\to\R^n$ and $\phi_2:V(T)\to\R^m$ be affine embeddings. Let $f:S\to T$ be a simplicial map. Define the geometric realization of $f$ to be the function $$\abs{f}:\abs{S}_{\phi_1}\to\abs{T}_{\phi_2}$$ given as follow. For each $x\in\abs{S}$, there exists $\lambda_v\in\R$ such that $x=\sum_{v\in T_1}\lambda_v\phi_1(v)$ and $\sum_{v\in V(S)}\lambda_v=1$. Then define $$\abs{f}(x)=\sum_{v\in V(S)}\lambda_v\psi(f(v))$$
\end{defn}

\begin{prp}{}{}\\
Let $S_1,S_2$ be simplicial complexes. Let $f:S\to T$ be a simplicial map. Then the following are true. 
\begin{itemize}
\item Let $\sigma\in S$. Then we have $$\abs{f}(\abs{\sigma})=\abs{f(\sigma)}$$
\item Let $\sigma\in S$. Then $\abs{f}|_{\abs{\sigma}}$ is affine linear. 
\end{itemize}
\end{prp}

\begin{lmm}{}{}\\
Let $S,T,U$ be simplicial complexes. Let $f:S\to T,g:T\to U$ be simplicial maps. Then we have $$\abs{g\circ f}=\abs{g}\circ\abs{f}$$
\end{lmm}

Thus geometric realization is a functor from a pair $(S,\phi)$ where $S$ is a simplicial complex on some set $T$ and $\phi$ is an affine embedding, to its geometric realization with respect to the embedding. 

\begin{defn}{Standard Geometric Realization of Abstract Simplicial Complexes}{}\\
Let $n\in\N^+$. Let $S$ be an abstract simplicial complex on $\{1,\dots,n\}$. Define the standard geometric realization of $S$ to be the space $$\abs{S}=\bigcup_{I\in S}\Delta^I$$ where $\Delta^I$ is the $\abs{I}$-simplex with vertices $e_i$ for $i\in I$. 
\end{defn}

By definition, the geometric realization $\abs{S}$ is a subcomplex of the standard $n$-simplex $[e_1,\dots,e_{n+1}]\subseteq\R^{n+1}$. 

\begin{eg}{}{}\\
Let $n\in\N$. Consider the set $S=\mP(\{1,\dots,n+1\})$. Then $\abs{S}$ is the standard $n$-simplex. 
\end{eg}

\subsection{Properties of Geometric Realization}
\begin{lmm}{}{}\\
Let $S$ be a simplicial complex. Then $\abs{S}$ and $\abs{\text{Bd}(S)}$ are homeomrophic. 
\end{lmm}

\begin{lmm}{}{}\\
Let $S$ be a simplicial complex. Let $p\in V(S)$. Then we have $$\abs{\text{st}(p)}=\bigcup_{F\in S, p\in F}\abs{F}$$
\end{lmm}

To be removed: 
\begin{prp}{}{}\\
Let $S$ be a simplicial complex. Let $\sigma\in S$. Let $p\in\abs{\sigma}^\circ$. Then we have $$H_\text{sing}^k(\abs{S},\abs{S}\setminus p)\cong\widetilde{H}_\Delta^{k-\abs{\sigma}}(\text{lk}(\sigma))$$
\end{prp}

\subsection{Simplicial Approximation}
\begin{defn}{Simplicial Approximations}{}\\
Let $S,T$ be simplicial complexes. Let $f:\abs{S}\to\abs{T}$ be a continuous map. A simplicial approximation of $f$ is a simplicial map $F:V(S)\to V(T)$ such that $\abs{F}$ is homotopic to $f$. 
\end{defn}

\begin{thm}{The Simplicial Approximation Theorem}{}\\
Let $S,T$ be simplicial complexes. Let $f:\abs{S}\to\abs{T}$ be a continuous map. Then there exists $n\in\N^+$ and a simplicial approximation $F:\text{Bd}^n(S)\to T$ of $f$. 
\end{thm}

\begin{prp}{}{}\\
Let $X$ be a CW complex. Then there exists a simplicial complex $S$ such that $\abs{S}$ is homotopic to $X$. 
\end{prp}

\subsection{Triangulated Spaces}
\begin{defn}{Triangulated Spaces}{}\\
Let $X$ be a space. We say that $X$ is triangulated if there is a simplicial complex $K$ such that $\abs{K}\cong X$. 
\end{defn}

\pagebreak
\section{Delta Complexes and Simplicial Homology}
\subsection{Delta Complexes}
\begin{defn}{$\Delta$-Complexes}{}\\
A $\Delta$-complex $(S_\bullet,d_\bullet)$ consists of the following data. 
\begin{itemize}
\item For each $n\in\N$, a set $S_n$ of $n$-simplicies. 
\item For each $n\in\N$ and $0\leq i\leq n$, a map $d_i^n:S_n\to S_{n-1}$ such that $$d_i^{n-1}\circ d_j^n=d_{j-1}^{n-1}\circ d_i^n$$ called the face relation is satisfied whenever $i<j$. 
\end{itemize}
\end{defn}

In particular, $d_i$ sends an $n$-simplex to its $i$th face, which is an $n-1$ simplex. This means that for $s=[v_1,\dots,v_n]\in S_n$, $d_i(s)=[v_1,\dots,\hat{v}_i,\cdots,v_n]$. 

\begin{prp}{}{} Every simplicial complex is a $\Delta$-complex. 
\end{prp}

\begin{defn}{Geometric Realization of $\Delta$-Complexes}{}\\
Let $S=(S_\bullet,d_\bullet)$ be a $\Delta$-set. The geometric realization of $S$ is a topological space $X$ that is built up inductively as follows: 
\begin{itemize}
\item The $0$-skeleton $X^0$ is a discrete set with points in $S_0$
\item Given the $n-1$ skeleton $X^{n-1}$ and $S_{n-1}$. Now define $$X^n=\left(X^{n-1}\cup\coprod_{\alpha\in I_n}\Delta_\alpha^n\right)/\sim$$ where $\sim$ is the equivalence relation $\Delta_\beta^{n-1}\sim\partial_k\Delta_\alpha^n$ given from the face maps $d_\bullet$. (Intuitively, each face of the $n$-simplex in $S_n$ gets identified with a $n-1$ simplex in $X^{n-1}$)
\item Define $X=\bigcup_nX^n$. The minimal $n$ for which $X=X^n$ is called the dimension of $n$
\end{itemize}
We also write $X$ as $\abs{S}$ to indicate the $\Delta$-set. 
\end{defn}

\begin{defn}{Map of Delta-Complexes}{} Let $S=(S_\bullet,d_\bullet)$ and $S'=(S_\bullet',d_\bullet')$ be two $\Delta$-sets. A map of $\Delta$-sets $f:S\to S'$ is a family of maps $f_n:S_n\to S_n'$ for each $n\in\N$ such that for all $0\leq i\leq n$, we have that $$d_i'\circ f_n=f_{n-1}\circ d_i:S_n\to S_{n-1}'$$
\end{defn}

\subsection{Delta Complex Structures}
$\Delta$-complexes act much nicer than CW complex because they are combinatorial. Indeed the attaching maps $d_j^n$ given by the $\Delta$-set are combinatorial in nature: we just need to define which element in $S_{n-1}$ each face of the simplex gets mapped to. In other words, $d_k^n:S_n\to S_{n-1}$ maps each $\Delta\in S_n$ to its $k$th face in $S_{n-1}$. 

\begin{thm}{}{} The geometric realization of a $\Delta$-complex is a CW complex
\end{thm}

However, most of the topological spaces are CW complexes rather than $\Delta$-complexes. Indeed the notion of CW complexes are less restrictive on the attaching maps, while that of $\Delta$-complexes are predetermined the face maps: one has to glue them so that each face of the $n$-simplexes is identified as an $n-1$ simplex in the existing skeleton. 

\begin{defn}{Delta-complex Structure}{} Let $X$ be a topological space. A $\Delta$-complex structure on $X$ is a $\Delta$-complex $S$ together with a homeomorphism $\abs{S}\cong X$. 
\end{defn}

The $\Delta$-complex structure is not unique. For example, one can have multiple ways of even defining a circle, depending the number of points. 

\subsection{Simplicial Homology}
The main goal is now to associate to every $\Delta$-complex an abelian group. This abelian group will serve as an invariant of the $\Delta$-complex. This is a two step process. To every $\Delta$-complex $S$, we associate a chain complex and then a collection of homology groups $$S\mapsto(\Delta_\bullet(S),\partial_\bullet)\mapsto H_\bullet(S)$$ with both steps being functorial in the sense that it respects associativity and identity when given a map of $\Delta$-complexes. \\

We begin by treating the case of $\Delta$-sets. 

\begin{defn}{Simplicial $n$-Chains}{} Let $S=(S_\bullet,d_\bullet)$ be a $\Delta$-set. Define the group of simplicial $n$-chains on $S$ to be free group $$\Delta_n(S)=\Z\langle\Delta_k^n\;|\;\Delta_k^n\in S_n\rangle$$ on the set of $n$-simplexes. An $n$ chain is then of the form $$\sum_km_k\Delta_k^n$$ for $m_k\in\Z$ and $\Delta_k^N\in S_n$. 
\end{defn}

All the simplicial $n$-chains are related by a formula called the boundary operator. 

\begin{defn}{Boundary Operator}{} Let $S$ be a $\Delta$-set. Define the boundary operator $\partial_n:\Delta_n(S)\to\Delta_{n-1}(S)$ by $$\partial_n(s)=\sum_{k=0}^n(-1)^kd_i^n(s)$$
\end{defn}

\begin{prp}{}{} The family of abelian groups $\Delta_n(S)$ of a $\Delta$-set $S$ and the boundary operator $\partial$ forms a chain complex $$(\Delta_\bullet(S),\partial_\bullet)$$ In particular, $\partial_n\circ\partial_{n+1}=0$ for all $n\in\N$ where $\partial_n$ is the boundary operator above. 
\begin{proof}
Let $s\in\Delta_{n+1}(X)$. Then we have that $$(\partial_n\circ\partial_{n+1})(s)=\partial_n\left(\sum_{j=0}^{n+1}(-1)^jd_j^{n+1}(s)\right)=\sum_{i=0}^n\sum_{j=0}^{n+1}(-1)^{i+j}d_i^n(d_j^{n+1}(s))$$ Fix a pair $0\leq i<j\leq n+1$. By definition 2.1.5, $A=(-1)^{i+j}d_i^n(d_j^{n+1}(s))$ and $B=(-1)^{i+j}d_{j-1}^n(d_i^{n+1}(s))$ cancel out. Moreover every summand is of the form $A$ or $B$ and not both so that the sum vanishes. In other words, we have that 
\begin{align*}
\sum_{i=0}^n\sum_{j=0}^{n+1}(-1)^{i+j}d_i^n(d_j^{n+1}(s))&=\sum_{0\leq i<j\leq n+1}(-1)^{i+j}d_i^n(d_j^{n+1}(s))+\sum_{0\leq j<i\leq n}(-1)^{i+j}d_i^n(d_j^{n+1}(s))\\
&=\sum_{0\leq i<j\leq n+1}(-1)^{i+j}d_{j-1}^n(d_i^{n+1}(s))+\sum_{0\leq j<i\leq n}(-1)^{i+j}d_i^n(d_j^{n+1}(s))\\
&=\sum_{0\leq i\leq j\leq n}(-1)^{i+j-1}d_j^n(d_i^{n+1}(s))+\sum_{0\leq j\leq i\leq n}(-1)^{i+j}d_i^n(d_j^{n+1}(s))\\
&=0
\end{align*}
We conclude that $(\Delta_\bullet(S),\partial_\bullet)$ forms a chain complex. 
\end{proof}
\end{prp}

Recalling from section $1$, we can now define the homology groups of the chain complex. 

\begin{defn}{The Simplicial Homology Groups}{} Let $S$ be a $\Delta$-set and $(\Delta_\bullet(S),\partial_\bullet)$ the chain complex of $S$. 
\begin{itemize}
\item Define the group of $n$-cycles to be $Z_n(S)=\ker(\partial_n)$
\item Define the group of $n$-boundaries to be $B_n(S)=\im(\partial_{n+1})$
\item Define the $n$th simplicial homology group to be the quotient $$H_n^\Delta(S)=\frac{Z_n(S)}{B_n(S)}=\frac{\ker(\partial_n)}{\im(\partial_{n+1})}=H_n(\Delta_\bullet(S))$$
\end{itemize}
\end{defn}

We will see different forms of chain complexes in the rest of the notes. In fact, we have seen from section $1$ that to every chain complex we can associate a sequence of homology groups. In our case of simplicial homology, there is a very topological way of understanding the homology groups. But this is not necessarily true for general homology groups built out of arbitrary chain complexes. 

\begin{defn}{Simplicial Homology Groups of a Geometric Realization}{} Let $X$ be a topological space with a $\Delta$-complex structure $\abs{S}\cong X$, define its $n$th simplicial homology group to be $$H_n^\Delta(X)=H_n^\Delta(S)$$
\end{defn}

By definition, $H_n^\Delta(X)$ would be nonzero exactly when the cycles in $X$, quotiented out with boundary of the faces is exactly the $n$ dimensional holes. This is because cycles in $X$ does not necessarily capture holes as there may be some faces within the cycles. Therefore we have to quotient out the cycles that encapture faces. \\

Since $H_n(C_\bullet)$ in general just means the homology groups of the chain complex $C_\bullet$, to distinguish between different homologies arising from different chain complexes, we will give special names for specific homology groups. In this instance, the simplicial homology of $X$ will be denoted $H_n^\Delta(X)$ instead of just $H_n(X)$. This is also to distinguish between other types of homology on $X$, such as singular homology. \\

We have seen that a map between two chain complexes induces a map between the homology groups. We can now define a map between two $\Delta$-sets so that they induce a map in chain complexes and in turn, a map in simplicial homology groups. 

\begin{lmm}{}{} A map of $\Delta$-sets $f_\bullet:S\to S'$ induces a chain map $f_\bullet:\Delta_\bullet(S)\to\Delta_\bullet(S')$. 
\begin{proof}
Define $f_n:\Delta_n(S)\to\Delta_n(S')$ on each generator $s\in S_n$ by $s\mapsto f_n(s)$ and extend it linearly on the free group. Then $f_n$ is naturally a group homomorphism. We now verify that $f_n$ together with the boundary operators satisfy the required commutativity. For $s\in S_n$, we have that 
\begin{align*}
\partial_n'(f_n(s))&=\sum_{k=0}^n(-1)^kd_k^n(f_n(s))\\
&=\sum_{k=0}^n(-1)^kf_{n-1}(d_k(s))\\
&=f_{n-1}\left(\sum_{k=0}^n(-1)^kd_k(s)\right)\\
&=f_{n-1}(\partial_n(s))
\end{align*}
and so we conclude. 
\end{proof}
\end{lmm}

Combining with the induced map in homology, we thus have that given a map of $\Delta$-sets, it induces a map in homology. 

\end{document}