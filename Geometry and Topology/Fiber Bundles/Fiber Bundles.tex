\documentclass[a4paper]{article}

\input{C:/Users/liula/Desktop/Latex/Headers/Headers.tex}

\pagestyle{fancy}
\fancyhf{}
\rhead{Labix}
\lhead{Fiber Bundles}
\rfoot{\thepage}

\title{Fiber Bundles}

\author{Labix}

\date{\today}
\begin{document}
\maketitle
\pagebreak
\tableofcontents

\pagebreak
\section{Fiber Bundles with a Group Structure}
\subsection{G-Bundles and the Structure Groups}
We would now like to enrich the structure of a fiber bundle with a group action on the fibers. Recall that a topological group is a group with the structure of a topology such that multiplication $(g_1,g_2)\mapsto g_1g_2$ and the inverse $g\mapsto g^{-1}$ are both continuous maps. 

\begin{defn}{G-Atlas}{}\\
Let $\pi:E\to B$ be a fiber bundle with fiber $F$. Let $G$ be topological group. A $G$-atlas on the fiber bundle consisting of the following data: 
\begin{itemize}
\item A continuous and faithful action of $G$ on $F$ (so that $G$ injects into $\text{Aut}(F)$). 
\item A set of local trivialization charts $$\{(U_k\subseteq B,\varphi_k:\pi^{-1}(U_k)\to U_k\times F)\;|\;k\in I\}$$ (where $B=\bigcup_{k\in I}U_k$ and $\varphi_k$ is a homeomorphism for all $k\in I$). 
\item A function of sets $g_{ij}:U_i\cap U_j\to\text{Aut}(F)$ for $i,j\in I$ called transition functions such that the following conditions are satisfied: 
\begin{itemize}
\item For $i,j\in I$, the map $g_{ij}$ lies in the injective image of $G$ in $\text{Aut}(F)$. 
\item For $i,j\in I$, the map $g_{ij}:U_i\cap U_j\to G$ is continuous. 
\item The transition maps $\psi_{ji}=\varphi_j\circ\varphi_i^{-1}:(U_i\cap U_j)\times F\to(U_i\cap U_j)\times F$ is given by $$\psi_{ji}(b,f)=\varphi_j\circ\varphi_i^{-1}(b,f)=(b,g_{ij}\cdot f)$$
\end{itemize}
\end{itemize}
\end{defn}

Notice that for non empty intersections $U_i\cap U_j$ for $U_i,U_j$ open sets in $B$, there is a well defined homeomorphism $$\varphi_j\circ\varphi_i^{-1}:(U_i\cap U_j)\times F\to(U_i\cap U_j)\times F$$ This is reminiscent of properties of an atlas on $M$. \\

\begin{defn}{Equivalent $G$-Atlas}{} Let $G$ be a topological group. Let $(F,E,B)$ be a fiber bundle and let $\{(U_i,\varphi_i\;|\;i\in I\}$ and $\{(V_j,\phi_j)\;|\;j\in J\}$ be two $G$-atlas on the fiber bundle. We say that they are equivalent if for all $x\in U_i\cap V_j$, $$\overline{g}_{ji}(x)=\phi_{j,x}\circ\phi_{i,x}^{-1}$$ is an element of $G$ and the map $$\overline{g}_{ji}:U_i\cap V_j\to G$$ defined by $x\mapsto\overline{g}_{ji}(x)$ is continuous. 
\end{defn}

It is clear that this is an equivalent relation. It is reflexive directly from definitions of a $G$-atlas. ???

\begin{defn}{G-Bundle}{} Let $G$ be a topological group. A $G$-bundle is a fiber bundle $(F,E,B)$ together with an equivalence class of $G$-atlas. In this case, $G$ is said to be the structure group of the fiber bundle. 
\end{defn}

Similar to the case of manifolds, an maximal atlas is exactly such an equivalence class of atlases. Therefore one can also think of a $G$-bundle as a fiber bundle equipped with a maximal $G$-atlas. 

\begin{eg}{}{}\\
Let $(F,E,B)$ be a vector bundle. Then $(F,E,B)$ is a $GL(F)$-bundle. 
\end{eg}

A morphism of $G$-bundles must now take into account of the group $G$. 

\begin{defn}{Morphisms of $G$-Bundles}{} Let $G$ be a topological group. Let $(F,E_1,B_1)$ and $(F,E_2,B_2)$ be two $G$-bundles with common fiber. A morphism of $G$-bundles is a morphism of bundles $$(\overline{h}:E_1\to E_2,h:B_1\to B_2)$$ such that the following are true. \\~\\

Let $\{(U_i,\varphi_i)\;|\;i\in I\}$ and $\{(V_j,\phi_j)\;|\;j\in J\}$ be local trivialities. 
\begin{itemize}
\item For all $x\in U_i\cap h^{-1}(V_j)$, the map $$\overline{g_{ji}}(x)=\phi_{j,x}\circ h|_{F_x}\circ\varphi_{i,x}^{-1}:F\to F$$ is an element of $G$. 
\item The map $$\overline{g_{ij}}:U_i\cap h^{-1}(V_j)\to G$$ defined by $x\mapsto\overline{g_{ji}}(x)$ is continuous. 
\end{itemize}
\end{defn}

Since morphisms of fiber bundles preserve fibers, morphisms of $G$-bundles also preserve fibers. 

It is easy to see that the mapping transformations $\widetilde{g_{ij}}$ satisfy the following two relations: 
\begin{itemize}
\item $\widetilde{g_{jk}}(x)\cdot g_{ij}(x)=\widetilde{g_{ik}}(x)$ for all $x\in U_i\cap U_j\cap h^{-1}(V_k)$
\item $g_{jk}'(h(x))\cdot\widetilde{g_{ij}}(x)=\widetilde{g_{ik}}(x)$ for all $x\in U_i\cap h^{-1}(V_j\cap V_k)$
\end{itemize}

$g_{jk}'$ here refers to the transition charts in $(F,E_2,B_2)$. \\~\\

\subsection{The Cocycle Condition}
Comparing this to the definition of fiber bundles, the point of the $G$-atlas is that the transition between local trivializations is governed by elements of the group $G$, whereas in a general fiber bundle we make no such assumptions. 

\begin{lmm}{}{}\\
Let $(F,E,B)$ be a fiber bundle and let $G$ be a topological group. Let $\{(U_k,\varphi_k)\;|\;k\in I\}$ be a $G$-atlas on $(F,E,B)$. Then the following are true regarding the coordinate transformations $g_{ji}(x)=\varphi_{j,x}\circ\varphi_{i,x}^{-1}$
\begin{itemize}
\item Cocycle condition: $g_{ki}(x)=g_{kj}(x)g_{ji}(x)$ for all $x\in U_i\cap U_j\cap U_k$. 
\item $g_{ji}(x)=(g_{ij}(x))^{-1}$ for all $x\in U_i\cap U_j$
\item $g_{ii}(x)=1$ for all $x\in U_i$
\end{itemize}
\end{lmm}

A collection of coordinate transformations satisfying the cocyle condition uniquely determines the structure of a fiber bundle. 

\begin{defn}{Associated Fiber Bundle}{}\\
Let $X$ be a space. Let $G$ be a topological group. Let $\{U_i\;|\;i\in I\}$ be an open cover. Let $g_{i,j}:U_i\cap U_j\to G$ be maps for each $i,j\in I$ such that $g_{k,i}(x)\circ g_{j,k}(x)\circ g_{i,j}(x)=1_G$ for all $x\in U_i\cap U_j\cap U_k$ and $g_{i,i}(x)=1_G$ for all $x\in U_i$. Let $F$ be a space equipped with a continuous action of $G$. Define the associated fiber bundle the data to be the space $$E_g=\frac{\coprod_{i\in I}U_i\times F}{\sim}$$ where $(i,b,x)\sim(j,b,g_{i,j}(b)(x))$ for $b\in U_i\cap U_j$, together with the quotient map induced by the universal property of quotients. 
\end{defn}

\begin{lmm}{}{}\\
Let $X$ be a space. Let $G$ be a topological group. Let $\{U_i\;|\;i\in I\}$ be an open cover. Let $g_{i,j}:U_i\cap U_j\to G$ be maps for each $i,j\in I$ such that $g_{k,i}(x)\circ g_{j,k}(x)\circ g_{i,j}(x)=1_G$ for all $x\in U_i\cap U_j\cap U_k$ and $g_{i,i}(x)=1_G$ for all $x\in U_i$. Let $F$ be a space equipped with a continuous action of $G$. Then $E_g$ is a $G$-bundle. 
\end{lmm}

\begin{lmm}{}{}\\
Let $X$ be a space. Let $G$ be a topological group. Let $p:E\to B$ be a $G$-bundle. Let $g_{i,j}$ be its transition functions. Then there is an isomorphism $$E_g\cong E$$ of $G$-bundles. 
\end{lmm}

\begin{thm}{Fiber Bundle Construction Theorem}{}\\
Let $X$ be a space. Let $G$ be a topological group. Then the above establishes a one to one $$\left\{\substack{G\text{-bundles over }B\\\text{with fiber }F}\right\}\;\;\;\;\overset{1:1}{\longleftrightarrow}\;\;\;\;\left\{\substack{\text{An open cover }\{U_i\;|\;i\in I\}\text{ of }B\\\text{Maps }\{g_{ij}:U_i\cap U_j\to G\;|\;i,j\in I\}\text{ satisfying the cocyle condition}\\\text{A space }F\text{ such that }G\text{ acts continuously on}}\right\}$$ correspondence 
\end{thm}

Just as the structure groups and trivialization charts determine the isomorphism type of a fiber bundle, the $\widetilde{g_{ij}}$ and a map of base space $h:B_1\to B_2$ completes determines a morphism of $G$-bundle. 

\begin{thm}{}{}\\
Let $(F,E_1,B_1)$ and $(F,E_2,B_2)$ be two $G$-bundles for a topological group $G$ with the same fiber $F$. Suppose that we have the following. 
\begin{itemize}
\item A map $h:B_1\to B_2$ of base space
\item A collection $\{\widetilde{g_{ij}}:U_i\cap h^{-1}(V_j)\to G\;|\;i\in I,j\in J\}$ of continuous maps such that \begin{gather*}
\widetilde{g_{jk}}(x)\cdot g_{ij}(x)=\widetilde{g_{ik}}(x)\;\;\;\;\text{ for all }\;\;\;\;x\in U_i\cap U_j\cap h^{-1}(V_k)\\
g_{jk}'(h(x))\cdot\widetilde{g_{ij}}(x)=\widetilde{g_{ik}}(x)\;\;\;\;\text{ for all }\;\;\;\;x\in U_i\cap h^{-1}(V_j\cap V_k)
\end{gather*}
\end{itemize}
Then there exists a unique $G$-bundle morphism having $h$ as the map of base space and having $\{\widetilde{g_{ij}}\;|\;i,j\in I\}$ as its mapping transformations. 
\end{thm}

\subsection{The Bundle Structure Theorem}
\begin{thm}{The Bundle Structure Theorem}{} Let $B$ be a topological group and let $G$ be a closed subgroup of $B$. Let $H$ be a closed subgroup of $G$. Suppose that $B/G$ has a local cross section at $G\in B/G$. Then the induced map of cosets $$p:B/H\to B/G$$ give a $G/H_0$-bundle with fiber $G/H$, where $$H_0=\bigcap_{g\in G}gHg^{-1}$$
\end{thm}

\begin{thm}{}{} Let $B$ be a topological group and let $G$ be a closed subgroup of $B$. Let $H$ be a closed subgroup of $G$. Then any two local cross sections of $B/G$ at $G\in B/G$ induces equivalent bundles. 
\end{thm}

\begin{crl}{}{} Let $B$ be a topological group and let $G$ be a closed subgroup of $B$. Then the projection map $p:B\to B/G$ is a $G$-bundle with fiber $G$. 
\end{crl}

\subsection{Reduction of the Structure Group}

\subsection{Bundle Structures in Equivariant Topology}
\begin{prp}{}{}\\
Let $G$ be a topological group. Let $H$ be a closed subgroup of $G$. Then the quotient map $G\to G/H$ is a fiber bundle with fiber $H$ and structure group $N_G(H)/H$. 
\end{prp}

\pagebreak
\section{Principal Bundles and Classifying Spaces}
\subsection{Point Set Topology Principal G-Bundles}
Let us recall some definitions from Groups and Rings. Let $G$ be a group acting on a set $X$. We say that a group action is free if $g\cdot x=x$ for all $x\in X$ implies that $g=1_G$. A group action is transitive if for any $x,y\in X$ there exists $g\in G$ such that $g\cdot x=y$. 

\begin{defn}{Principal Bundles}{}\\
Let $G$ be a topological group. A principal $G$-bundle is a $G$-bundle $(F,E,B)$ together with a continuous group action $G$ on $E$ such that the following are true. 
\begin{itemize}
\item The action of $G$ preserves fibers. This means that $g\cdot x\in E_b$ if $x\in E_b$. (This also means that $G$ is a group action on each fiber)
\item The action of $G$ on each fiber is free and transitive
\item The map $G\to F$ defined by sending $g\mapsto g\cdot x$ for any $x\in X$ is a homeomorphism. 
\item Local triviality condition: Each local triviality map $\varphi_U:p^{-1}(U)\to U\times F$ are $G$-equivariant maps. 
\end{itemize}
\end{defn}

Recall that even if $G$ acts freely and transitively on $X$, $G$ is still not homeomorphic to $X$. This is because the map $G\to X$ defined by $g\mapsto g\cdot x$ for any $x\in X$ is continuous and bijective. One usually requires that either $G$ is compact or more in general, the above map is an open map then $G$ will be homeomoprhic to $X$. 

\begin{eg}{}{}\\
The following are true regarding the stiefel and grassmannian manifolds. 
\begin{itemize}
\item The projection map $V_k(\R^n)\to\text{Gr}_k(\R^n)$ is a principal $\text{GL}(k,\R)$-bundle. 
\item The projection map $V_k(\C^n)\to\text{Gr}_k(\C^n)$ is a principal $\text{GL}(k,\C)$-bundle. 
\end{itemize}
\end{eg}

\begin{eg}{}{}\\
The projection map $S^{2n+1}\to\C\Prj^n$ is a principal $S^1$-bundle. 
\end{eg}

\begin{lmm}{}{}\\
Let $G$ be a topological group. Let $p:E\to B$ be a principal $G$-bundle. Then $p$ is a $G$-bundle. 
\end{lmm}

For those who know what homogenous spaces are, principal bundles are $G$-bundles such that $F$ is a principal homogenous space for the left action of $G$ itself. 

\begin{prp}{}{} Let $G$ be a topological group. Let $p:E\to B$ be a principal $G$-bundle. Then there is a homeomorphism $$E/G\cong B$$ where $E/G$ is the orbit space. 
\end{prp}

Conversely, given a continuous group action on a space, we can ask in what conditions will the space be a principal bundle over the orbit space. 

\begin{prp}{}{} Let $X$ be a space with a free $G$ action. Let $p:X\to X/G$ be the projection map to the orbit space. If for all $x\in X/G$, there is a neighbourhood $U$ of $x$ and a continuous map $s:U\to X$ such that $p\circ s=\text{id}_U$, then $(G,X,X/G)$ is a principal $G$-bundle. 
\end{prp}

This proposition essentially means that if for each point in $X/G$, there is a local section, then it is sufficient for $X$ to be a principal $G$ bundle over $X/G$. 

\begin{prp}{}{} Let $X$ be a space. Let $p:\tilde{X}\to X$ be a covering space. If $p$ is a normal covering space, then $p$ is a principal $G$-bundle for some group $G$. 
\end{prp}

\subsection{Morphisms of Principal G-Bundles}
\begin{defn}{Morphism of Principal Bundles}{} Let $G$ be a topological group. Let $p_1:E_1\to B_1$ and $p_2:E_2\to B_2$ be two prinicipal $G$-bundle. A morphism of principal $G$-bundles is a morphism of fiber bundles $(\tilde{f},f)$ such that $\tilde{f}$ is a $G$-equivariant map. 
\end{defn}

\begin{defn}{Isomorphism of Principal Bundles}{} Let $G$ be a topological group. Let $p_1:E_1\to B_1$ and $p_2:E_2\to B_2$ be two prinicipal $G$-bundle. We say that they are isomorphic if there exists a morphism of principal bundles $(\tilde{f},f)$ such that it is an isomorphism in the sense of 1.1.6. 
\end{defn}

Principal bundles enjoy a very unique property. If one can construct a morphism between two principal bundles then the structure of a fiber as the structure group ensures that the two bundles are isomorphic. 

\begin{prp}{}{} Every morphism of principal bundles is an isomorphism of principal bundles. 
\end{prp}

\begin{prp}{}{} Let $G$ be topological group. A principal $G$-bundle is trivial if and only if it admits a global section. 
\end{prp}

This is entirely untrue for general bundles. For examples, the zero section of a fiber bundle is a global section. 

\subsection{Associated Principal Bundles}
Principal bundles are easier to understand than fiber bundles with structure group because their fibers are isomorphic to the group $G$ and its action is just left translations. If we are able to translate some properties of fiber bundles to principal bundles we can simplify proofs. 

\begin{prp}{}{}\\
Let $G$ be a topological group. Let $F$ be a space. Let $p:E\to B$ be a $G$-bundle with fiber $F$. Let $g_{i,j}$ be the transition functions of the bundle. Then the associated bundle $E_g$ is a principal $G$-bundle. 
\end{prp}

\begin{prp}{}{}\\
Let $G$ be a topological group. Let $F_1,F_2$ be spaces. Let $p_1:E_1\to B$ and $p_2:E_2\to B$ be $G$-bundles with fibers $F_1$ and $F_2$. Then $p_1$ and $p_2$ are isomorphic if and only if their associated principal $G$-bundles are isomorphic. 
\end{prp}

\begin{crl}{}{} A fiber bundle is equivalent to the trivial bundle if and only if its associated principal bundle admits a cross section. 
\end{crl}

\subsection{Milnor's Join Construction}
\begin{defn}{The Join of two Spaces}{}\\
Let $X,Y$ be spaces. Define the join of $X$ and $Y$ to be $$X\ast Y=\frac{X\times I\times Y}{\sim}$$ where $(x,0,y_1)\sim(x,0,y_2)$ for $x\in X$ and $y_1,y_2\in Y$ and $(x_1,1,y)\sim(x_2,1,y)$ for $x_1,x_2\in X$ and $y\in Y$. 
\end{defn}

\begin{eg}{}{}\\
Let $X$ be a space. Then the following are true. 
\begin{itemize}
\item $X\ast\{p\}=CX$. 
\item $X\ast\{p_1,p_2\}=\Sigma X$. 
\end{itemize}
\end{eg}

\begin{eg}{}{}\\
Let $n,m\in\N$. Then we have $$S^n\ast S^m\cong S^{n+m+1}$$
\end{eg}

\begin{eg}{}{}\\
We have $x_0\ast\cdots\ast x_n\cong\Delta^n$. 
\end{eg}

\begin{lmm}{}{}\\
Let $X,Y$ be spaces. Then the inclusions $i_X,i_Y:X,Y\to X\ast Y$ are null homotopic. 
\end{lmm}

\begin{defn}{Milnor's Join Construction}{}\\
Let $G$ be a topological group. Define the Milnor join construction of $G$ to be $$EG=\lim_{k\to\infty}G^{\ast(k+1)}$$
\end{defn}

\begin{lmm}{}{}\\
Let $G$ be a topological group. Then the following are true. 
\begin{itemize}
\item $EG$ is an $G$-equivariant CW complex. 
\item $EG$ is contractible. 
\item The projection map $EG\to EG/G$ is a universal principle $G$-bundle. 
\end{itemize}
\end{lmm}

\subsection{Universal Principal Bundles}
Recall that homotopic maps give isomorphic fiber bundles. (???: Descends to isomorphic principal $G$-bundles)

\begin{defn}{Principal Bundle Functor}{} Let $G$ be a topological group and $X$ a space. Define a contravariant functor $\text{Prin}_G:\bold{hTop}\to\bold{Set}$ as follows. 
\begin{itemize}
\item For $X$ a topological space, $\text{Prin}_G(X)$ is the set of isomorphism classes of principal $G$-bundles over $X$. 
\item If $[f:X\to Y]$ is a homotopy class of continuous maps, $$\text{Prin}_G([f]):\text{Prin}_G(Y)\to\text{Prin}_G(X)$$ is defined as follows. If $[p:E\to Y]$ is an homotopy class of principal $G$-bundles over $Y$, then it is sent to $[f^\ast(E)]$ the homotopy class of the pullback of $p$. 
\end{itemize}
\end{defn}

\begin{prp}{}{}\\
Let $G$ be a topological group. Then the principal bundle functor restricted to CW complexes is representable via the nerve of the category $BG$. Explicitly, this means that there exists a principal $G$-bundle $EG\to BG$ together with a natural isomorphism $$\psi:[X,BG]\to\text{Prin}_G(X)$$ This natural isomorphism is defined by $f\mapsto[f^\ast(EG)]$. (Milnor's Join Construction)
\end{prp}

Note that the theorem is still true when the principal bundle functor is restricted to paracompact spaces (with the same universal object). 

\begin{defn}{Universal G-Bundles}{}\\
Let $G$ be a topological group. A principal $G$-bundle $(F,E,B)$ is said to be universal if it represents the principal bundle functor. 
\end{defn}

\begin{thm}{}{}\\
Let $(F,E,B)$ be a principal $G$-bundle. If $E$ is contractible then $(F,E,B)$ is a universal $G$-bundle. 
\end{thm}

A surprising thing is that $BG$ is not determined by its isomorphism type but instead by the weaker condition of its homotopy type. 

\begin{prp}{}{}\\
Let $G$ be a topological group. Let $p:E\to B$ be a universal $G$-bundle. Then the following are true. 
\begin{itemize}
\item $B$ is weakly equivalent to a CW complex (hence WLOG we may always take the base space of a universal $G$-bundle to be a CW complex). 
\item Let $p:E\to B$ be a universal $G$-bundle such that $B$ is a CW complex. Then $B$ is unique up to canonical homotopy equivalence. 
\item $E$ is unique up to $G$-homotopy equivalence. 
\end{itemize}
\end{prp}

\subsection{Properties of the Classifying Space}
\begin{prp}{}{}\\
Let $G$ be a topological group. Let $BG$ be the classifying space of $G$. Then there is a homotopy equivalence $$G\simeq\Omega BG$$
\end{prp}

\begin{crl}{}{}\\
Let $G$ be a topological group. Then there is a natural bijection $$\text{Prin}_G(S^n)\cong\pi_{n-1}(G)$$
\end{crl}

\begin{crl}{}{}\\
Let $G$ be a discrete topological group. Then the classifying space $BG$ is a $K(G,1)$-space. 
\end{crl}

\begin{eg}{}{}\\
There is a homotopy equivalence $B\Z\simeq S^1$. 
\end{eg}

\begin{eg}{}{}\\
There are homotopy equivalences $$K(\Z/2\Z,1)\simeq B\Z/2\Z\simeq\R\Prj^\infty$$
\end{eg}

\pagebreak
\section{Vector Bundles and Principal Bundles}
\subsection{Relation to Principal Bundles}
\begin{defn}{Frame Bundle}{}\\
Let $F$ be a field. Let $B$ be a space. Let $p:E\to B$ be a vector bundle over $F$. Define the frame bundle $F(E)$ of $p$ to be the associated bundle of $p$. 
\end{defn}

Note that this is not the same as the associated vector bundle of $p$, which is isomorphic to $p$ itself. Explicitly, the total space of the frame bundle of a vector bundle $p:E\to B$ of rank $n$ is given by $$F(E)=\frac{\coprod_{i\in I}U_i\times\text{GL}_n(F)}{\sim}$$ where $(i,b,v)\sim(j,b,g_{i,j}(b)(v))$ and $g_{i,j}:U_i\cap U_j\to\text{GL}_n(F)$ are the transition functions. \\

Since vector bundles are just fiber bundles with structure group the general linear group, it follows that frame bundles are principal bundles over their structure group. 

\begin{prp}{}{}\\
Let $X$ be a space. Then the following are true. 
\begin{itemize}
\item There is a natural bijection $$\phi:\text{Prin}_{\text{GL}(n,\R)}(X)\overset{\cong}{\longrightarrow}\text{Vect}_n^\R(X)$$ given by mapping $p:E\to B$ to the frame bundle $F(E)$.
\item There is a natural bijection $$\phi:\text{Prin}_{\text{GL}(n,\C)}(X)\overset{\cong}{\longrightarrow}\text{Vect}_n^\C(X)$$ given by the same map. 
\end{itemize}
\end{prp}

\subsection{Reducing the Structure Group of Vector Bundles}
\begin{prp}{}{}\\
Let $X$ be a paracompact space. Then the following are true. 
\begin{itemize}
\item There is a natural bijection $$\phi:\text{Prin}_{O(n)}(X)\overset{\cong}{\longrightarrow}\text{Vect}_n^\R(X)$$ given by mapping $p:E\to B$ to the frame bundle $F(E)$. 
\item There is a natural bijection $$\phi:\text{Prin}_{U(n)}(X)\overset{\cong}{\longrightarrow}\text{Vect}_n^\C(X)$$ given by the same map. 
\end{itemize}
\end{prp}

\begin{eg}{}{}\\
There are natural bijections $$\text{Vect}_1^\R(X)\cong\text{Prin}_{\text{GL}(n,\R)}(X)\cong\text{Prin}_{O(n)}(X)\cong H^1(X;\Z/2\Z)$$
\end{eg}

\begin{eg}{}{}\\
There are natural bijections $$\text{Vect}_1^\C(X)\cong\text{Prin}_{\text{GL}(n,\C)}(X)\cong\text{Prin}_{U(n)}(X)\cong H^2(X;\Z)$$
\end{eg}
<<<<<<< HEAD

\begin{eg}{}{}\\
The following are true. 
\begin{itemize}
\item The only non-trivial line bundle of $\R\Prj^n$ is given by the universal line bundle $\gamma_1$. 
\item The only non-trivial line bundles of $\C\Prj^n$ is given by $\gamma_1^{\otimes n}$ or $(\gamma_1^\ast)^{\otimes n}$ for some $n\geq 0$ where $\gamma_1$ is the universal line bundle. 
\end{itemize}
\end{eg}

\begin{prp}{}{}\\
Let $n\in\N$. Then following are true. 
\begin{itemize}
\item There is a homotopy equivalence $$B\text{GL}(n,\R)\simeq BO(n)$$
\item There is a homotopy equivalence $$B\text{GL}(n,\C)\simeq BU(n)$$
\end{itemize}
\end{prp}

\begin{prp}{}{}\\
Let $n\in\N$. Then the following are true. 
\begin{itemize}
\item We have $BO(n)\cong\text{Gr}_n(\R^\infty)$. 
\item We have $BU(n)\cong\text{Gr}_n(\C^\infty)$. 
\end{itemize}
\end{prp}

\begin{prp}{}{} The following are true regarding the classifying space of the limits of the orthogonal and unitary groups. 
\begin{itemize}
\item There is a homeomorphism $BO\cong\lim_{n\to\infty}BO(n)$
\item There is a homeomorphism $BU\cong\lim_{n\to\infty}BU(n)$
\end{itemize}
\end{prp}
=======
>>>>>>> f8d03f5771b3a883668b64bba3c935b61510827b

\begin{eg}{}{}\\
The following are true. 
\begin{itemize}
\item The only non-trivial line bundle of $\R\Prj^n$ is given by the universal line bundle $\gamma_1$. 
\item The only non-trivial line bundles of $\C\Prj^n$ is given by $\gamma_1^{\otimes n}$ or $(\gamma_1^\ast)^{\otimes n}$ for some $n\geq 0$ where $\gamma_1$ is the universal line bundle. 
\end{itemize}
\end{eg}

\begin{prp}{}{}\\
Let $n\in\N$. Then following are true. 
\begin{itemize}
\item There is a homotopy equivalence $$B\text{GL}(n,\R)\simeq BO(n)$$
\item There is a homotopy equivalence $$B\text{GL}(n,\C)\simeq BU(n)$$
\end{itemize}
\end{prp}

\begin{prp}{}{}\\
Let $n\in\N$. Then the following are true. 
\begin{itemize}
\item We have $BO(n)\cong\text{Gr}_n(\R^\infty)$. 
\item We have $BU(n)\cong\text{Gr}_n(\C^\infty)$. 
\end{itemize}
\end{prp}

\begin{prp}{}{} The following are true regarding the classifying space of the limits of the orthogonal and unitary groups. 
\begin{itemize}
\item There is a homeomorphism $BO\cong\lim_{n\to\infty}BO(n)$
\item There is a homeomorphism $BU\cong\lim_{n\to\infty}BU(n)$
\end{itemize}
\end{prp}


\pagebreak
\section{Characteristic Classes}
\subsection{Characteristic Classes as a Ring}
\begin{defn}{Characteristic Classes}{} Let $G$ be a topological group and $X$ a space. Denote $\text{Prin}_G(X)$ the isomorphism classes of principal $G$-bundles over $X$. Let $H^\ast(-)$ be a cohomology functor. A characteristic class for $G$ is a natural transformation $$c:\text{Prin}_G(-)\Rightarrow H^\ast(-)$$
\end{defn}

Explicitly, if $p:E\to X$ is a principal $G$-bundle, then $c$ assigns $p$ to a cohomology class $c(p)\in H^\ast(X)$. 

\begin{eg}{}{}\\
Let $G$ be a topological group. Suppose that $c$ is a characteristic class. Let $B$ be a space. Suppose that $\varepsilon:B\times G\to B$ is the trivial principal $G$-bundle. Then we have $$c(\varepsilon)=1$$
\end{eg}

\begin{defn}{Ring of Characteristic Classes}{} Let $G$ be a topological group. Let $R$ be a commutative ring. Define $\text{Char}_G(R)$ to be the set of all characteristic classes for principal $G$-bundles that take values in $H^\ast(-;R)$. 
\end{defn}

\begin{prp}{}{} Let $G$ be a topological group. Let $R$ be a commutative ring. Then $\text{Char}_G(R)$ is a ring with unit the constant characteristic class. 
\end{prp}

\begin{thm}{}{} Let $G$ be a topological group and let $R$ be a commutative ring. Then there is an isomorphism $$\text{Char}_G(R)\cong H^\ast(BG;R)$$
\end{thm}

\subsection{The Stiefel-Whitney Class}
\begin{defn}{The Stiefel-Whitney Class}{} Consider the group $O(n)$. Let $$w_i:\text{Prin}_{O(n)}(-)\to H^i(-,\Z/2\Z)$$ be a collection of natural transformations. We say that they form a Stiefel-Whitney class if the following are satisfied. 
\begin{enumerate}
\item Rank: If $E$ is a principal $O(n)$-bundle, then $w_0(E)=1$ and $w_i(E)=0$ for $i>\rank(E)$. 
\item Naturality: Let $p:E\to X$ be a principal $O(n)$-bundle and let $f:Y\to X$ be a map. Then $$w_i(f^\ast(E))=f^\ast(w_i(E))$$
\item Whitney Product Formula: If $E_1,E_2$ are principal $O(n)$-bundles, then $$w_k(E_1\oplus E_2)=\sum_{i=0}^kw_i(E_1)\smile w_{k-i}(E_2)$$
\item Normalization: If $\gamma$ is the tautological line bundle over $\Prj^1(\R)$, then $w_1(\gamma)$ is non-zero. 
\end{enumerate}
\end{defn}

\begin{prp}{}{}\\\
The Stiefel-Whitney class exists and is unique. 
\end{prp}

\begin{defn}{The Total Stiefel-Whitney Class}{}\\
Define the total Stiefel-Whitney class to be the natural transformation $$w=\sum_{k=0}^\infty:\text{Prin}_{O(n)}(-)\Rightarrow H^\ast(-,\Z/2\Z)$$
\end{defn}

\begin{lmm}{}{}\\
Let $E_1,E_2$ be principal $O(n)$ bundles. Then we have $$w(E_1\oplus E_2)=w(E_1)w(E_2)$$
\end{lmm}

\begin{prp}{}{} The following are true regarding the Stiefel-Whitney class. 
\begin{itemize}
\item If $p_1:E_1\to B_1$ and $p_2:E_2\to B_2$ are isomorphic principal $O(n)$-bundles, then $w(E_1)=w(E_2)$
\item If $e=B\otimes\R^n$ is the trivial bundle, then $w(e\oplus E)=w(E)$ for any principal $O(n)$-bundle $E$. 
\item 
\end{itemize}
\end{prp}

\begin{prp}{}{}\\
Let $n\in\N^+$. Then there is a natural isomorphism $$\text{Char}_{O(n)}(\Z/2\Z)\cong\Z/2\Z[x_1,\dots,x_n]$$ where $\deg(x_i)=1$ and the isomorphism is given by $x_i\mapsto w_i$ the $i$th Stiefel-Whitney class. 
\end{prp}

\begin{prp}{}{} Let $X$ be a space. Then the function $$w_1:\text{Vect}_1^\R(X)\to H^1(X;\Z/2\Z)$$ is a homomorphism. It is an isomorphism if $X$ is homotopy equivalent to a CW complex. 
\end{prp}

\begin{crl}{}{} Let $X$ be homotopy equivalent to a CW complex. Then any vector bundle $E\to X$ is orientable if and only if $w_1(E)=0$. 
\end{crl}

\begin{prp}{}{}\\
Let $E$ be a principle $O(n)$-bundle. If $E$ has $k$ nowhere vanishing sections, then we have $w_{n-k+1}(E)=\cdots=w_n(E)=0$
\end{prp}

\subsection{The Chern Class}
\begin{defn}{The Chern Class}{} Consider the group $U(n)$. Let $$c_i:\text{Prin}_{U(n)}(-)\to H^{2i}(-,\Z)$$ be a collection of natural transformations. We say that they form a Chern class if the following are satisfied. 
\begin{enumerate}
\item Rank: If $E$ is a principal $U(n)$-bundle, then $c_0(E)=1$ and $c_i(E)=0$ for $i>\rank(E)$. 
\item Naturality: Let $p:E\to X$ be a principal $O(n)$-bundle and let $f:Y\to X$ be a map. Then $$c_i(f^\ast(E))=f^\ast(c_i(E))$$
\item Whitney Product Formula: If $E_1,E_2$ are principal $O(n)$-bundles, then $$c_k(E_1\oplus E_2)=\sum_{i=0}^kc_i(E_1)\smile c_{k-i}(E_2)$$
\item Normalization: If $\gamma$ is the tautological line bundle over $\Prj^1(\R)$, then $c_1(\gamma)$ is non-zero. 
\end{enumerate}
\end{defn}

\begin{prp}{}{} The Chern class exists and is unique. 
\end{prp}

\begin{defn}{The Total Chern Class}{}\\
Define the total Chern class to be the natural transformation $$c=\sum_{k=0}^\infty c_k:\text{Prin}_{O(n)}(-)\to H^\ast(-,\Z)$$
\end{defn}

\begin{lmm}{}{}\\
Let $E_1,E_2$ be principal $U(n)$ bundles. Then we have $$c(E_1\oplus E_2)=c(E_1)c(E_2)$$
\end{lmm}

\begin{prp}{}{}\\
Let $B$ be a space. Let $p:E\to B$ be a vector bundle of rank $n$ with fiber $\C$. Then $c_1(E)=0$ if and only if $E$ admits a reduction of structure group to $SU(n)$. 
\end{prp}

TBA: First chern class is complete invariant of complex line bundles. First Stiefel-Whitney class is a complete invariant of real line bundle. 

\begin{prp}{}{}
Let $n\in\N^+$. Then there is a natural isomorphism $$\text{Char}_{U(n)}(\Z)\cong\Z[x_1,\dots,x_n]$$ where $\deg(x_i)=2$ and the isomorphism is given by $x_i\mapsto c_i$ the $i$th Chern class. 
\end{prp}

Combined with theorem 5.1.3, this means that we can now compute the cohomology of the  classifying space of $U(n)$. Namely, we have an isomorphism $$H^\ast(BU(n);\Z)\cong\Z[c_1,\dots,c_n]$$

\begin{prp}{}{} Let $X$ be a space. Then the function $$c_1:\text{Vect}_1^\C(X)\to H^2(X;\Z)$$ is a homomorphism. It is an isomorphism if $X$ is homotopy equivalent to a CW complex. 
\end{prp}

\begin{thm}{The Splitting Principle}{}\\
Let $B$ be a space. Let $p:E\to B$ be a vector bundle of rank $n$ with fiber $\C$. Then there exists a space $F(B)$ and a map $\pi:F(B)\to B$ such that the following are true. 
\begin{itemize}
\item $\pi^\ast:H^\ast(B;\Z)\to H^\ast(F(B);\Z)$ is injective. 
\item $\pi^\ast(E)$ is a direct sum of line bundles. 
\end{itemize}
\end{thm}

\subsection{The Pontryagin Class}
\begin{defn}{The Pontryagin Class}{}\\
Let $B$ be a space. Let $p:E\to B$ be a vector bundle over $\R$. Define the Pontryagin class of $E$ to be $$p_i(E)=(-1)^ic_{2i}(E\otimes_\R\C)\in H^{4i}(-,\Z)$$
\end{defn}

\begin{defn}{The Total Pontryagin Class}{}\\
Let $B$ be a space. Let $p:E\to B$ be a vector bundle over $\R$. Define the total Pontryagin class of $E$ to be $$p(E)=\sum_{i=0}^\infty p_i(E)$$
\end{defn}

\begin{prp}{}{}\\
Let $B$ be a space. Let $E_1$ and $E_2$ be two vector bundles over $\R$. Then we have $$2(p(E_1\oplus E_2)-p(E_1)p(E_2))=0\in H^\ast(B,\Z)$$
\end{prp}

\pagebreak
\section{More on Orientations of Vector Bundles}
\subsection{The Orientation Cover}
\begin{lmm}{}{}\\
Let $V$ be a finite dimensional vector space over $\R$. Then there is a bijection $$\{[w]\;|\;[w]\text{ is an orientation of }V\}\;\;\overset{1:1}{\longleftrightarrow}\;\;H^n(V,V\setminus\{0\};\Z)$$
\end{lmm}

\begin{defn}{The Orientation Cover}{}\\
Let $B$ be a space. Let $p:E\to B$ be a vector bundle of rank $n$ with fiber $\R$. Define the orientation cover to be the space $$\widetilde{E}=\{(b,w_b)\;|\;b\in B, w_b\text{ is an orientation of }E_b\}$$
\end{defn}

Since orientations are in bijection with $H^n(V,V\setminus\{0\};\Z)$, we may equivalently define the orientation cover using elements of the top cohomology of the fiber bundle. This viewpoint is useful in generalizing orientation to arbitrary coefficient rings. 

\begin{lmm}{}{}\\
Let $B$ be a space. Let $p:E\to B$ be a vector bundle of rank $n$ with fiber $\R$. Then $\widetilde{E}$ is a covering space of $B$. 
\end{lmm}

\begin{prp}{}{}\\
Let $B$ be a space. Let $p:E\to B$ be a vector bundle of rank $n$ with fiber $\R$. Then the following are equivalent. 
\begin{itemize}
\item $E$ is orientable. 
\item $E$ admits a reduction of structure groups to $\text{SL}(n,\R)$. 
\item $\text{Fr}(E)\times_{\text{GL}(n,\R)}\{\pm1\}$ admits a global section. 
\item $w_1(E)=0$. 
\item $\Lambda^n(E)$ is isomorphic to the trivial bundle. 
\item $\widetilde{E}$ admits a global section. 
\item $\widetilde{E}$ is isomorphic to the trivial bundle. 
\item There exists a class $u\in H^n(E,E\setminus B;\Z)$ where $B$ is identified with the zero section of $E$, such that the image of $u$ in the map $H^n(E,E\setminus B;\Z)\to H^n(E_b,E_b\setminus\{0\};\Z)\cong\Z$ induced by inclusion is a generator. 
\end{itemize}
\end{prp}

\begin{defn}{The R-Orientation Cover}{}\\
Let $B$ be a space. Let $p:E\to B$ be a vector bundle of rank $n$ with fiber $\R$. Let $R$ be a ring. Define the $R$-orientation cover of $E$ to be $$\widetilde{E}_R=\{(b,w_b)\;|\;b\in B, w_b\in H^n(E_b,E_b\setminus\{0\};R)\cong R\text{ is a generator }\}$$
\end{defn}

\begin{lmm}{}{}\\
Let $B$ be a space. Let $p:E\to B$ be a vector bundle of rank $n$ with fiber $\R$. Let $R$ be a ring. Then $\widetilde{E}_R$ is a covering space of $B$. 
\end{lmm}

\begin{defn}{R-Orientability}{}\\
Let $B$ be a space. Let $p:E\to B$ be a vector bundle of rank $n$ with fiber $\R$. Let $R$ be a ring. We say that $E$ is $R$-orientable if $E$ admits a global section. 
\end{defn}

\begin{defn}{The Thom Class}{}\\
Let $B$ be a space. Let $p:E\to B$ be a vector bundle of rank $n$ with fiber $\R$. Let $R$ be a ring. Let $s_0:b\to E$ denote the zero section. Define the Thom class of $E$ to be any class $$u\in H^n(E,E\setminus s_0(B);R)$$ such that the image of $u$ in the map $H^n(E,E\setminus s_0(B);R)\to H^n(E_b,E_b\setminus\{0\};R)\cong R$ induced by the inclusion is a generator. 
\end{defn}

\begin{lmm}{}{}\\
Let $B$ be a space. Let $p:E\to B$ be a vector bundle of rank $n$ with fiber $\R$. Let $R$ be a ring. Then the following are equivalent. 
\begin{itemize}
\item $E$ is $R$-orientable. 
\item $\widetilde{E}$ is isomorphic to the trivial bundle. 
\item $E$ admits a Thom class. 
\end{itemize}
\end{lmm}

\begin{crl}{}{}\\
Let $B$ be a space. Let $p:E\to B$ be a vector bundle of rank $n$ with fiber $\R$. Then $E$ is $\Z/2\Z$-orientable. 
\end{crl}

\subsection{The Euler Class}
\begin{defn}{The Euler Class}{}\\
Let $B$ be a space. Let $p:E\to B$ be an orientable vector bundle of rank $n$ with fiber $\R$. Let $s_0:B\to E$ be the zero section. Let $u\in H^n(E,E\setminus s_0(B);\Z)$ be a Thom class of $E$. Define the Euler class of $E$ to be $$e(E)=s_0^\ast(u)\in H^n(B;\Z)$$
\end{defn}

\begin{prp}{}{}\\
Let $B$ be a space. Let $p:E\to B$ be an orientable vector bundle with fiber $\R$. Then the following are true. 
\begin{itemize}
\item If $f:X\to B$ is a map, then $e(f^\ast(E))=f^\ast(e(E))$. 
\item If $q:F\to B$ is an orientable vector bundle, then $e(E\oplus F)=e(E)\smile e(F)$. 
\end{itemize}
\end{prp}

\begin{crl}{}{}\\
Let $B$ be a space. Let $p:E\to B$ be a orientable vector bundle of rank $n$ with fiber $\R$. If $n$ is odd, then $e(E)\smile e(E)=0$. 
\end{crl}

\begin{prp}{}{}\\
Let $B$ be a space. Let $p:E\to B$ be an orientable vector bundle of rank $n$ with fiber $\R$. Let $f:H^n(B;\Z)\to H^n(B;\Z/2\Z)$ be the change of rings homomorphism. Then we have $f(e(E))=w_n(E)$. 
\end{prp}

\begin{prp}{}{}\\
Let $B$ be a space. Let $p:E\to B$ be a vector bundle of rank $n$ with fiber $\R$. If $E$ admits a nowhere vanishing section, then $e(E)=0$. 
\end{prp}

\subsection{Classification of Characteristic Classes of Oriented Vector Bundles}
def of tautological bundle $\gamma_{2n}$ over $BSO(2n)$. 

\begin{prp}{}{}\\
Let $R$ be an integral domain such that $2$ is invertible. Let $n\in\N$. Then we have $$H^\ast(BSO(2n+1);R)\cong R[p_1,\dots,p_n]\;\;\;\text{ and }\;\;\;H^\ast(BSO(2n);R)\cong R[p_1,\dots,p_n,e(\gamma_{2n})]$$
\end{prp}

\pagebreak
\section{Thom Space and the Thom Isomorphism Theorem}
\subsection{Thom Space}
\begin{defn}{Unit Sphere and Unit Disc Bundle}{} Let $p:E\to B$ be an $n$-dimensional vector bundle over $\R$. Let $\langle-,-\rangle:E\times E\to\R$ be a smoothly varying inner product on $E$. Define the disc bundle to be $$D(E)=\{e\in E\;|\;\langle e,e\rangle\leq 1\}$$ together with the map $p|_{D(E)}:D(E)\to B$. Define the sphere bundle to be $$S(E)=\{e\in E\;|\;\langle e,e\rangle=1\}$$ together with the map $p|_{S(E)}:S(E)\to B$. 
\end{defn}

\begin{defn}{Thom Space}{} Let $p:E\to B$ be an $n$-dimensional vector bundle over $\R$ such that $B$ is paracompact. Define the Thom space of $E$ to be $$T(E)=\frac{D(E)}{S(E)}$$ The base point is taken as the equivalent class $S(E)$ if needed. 
\end{defn}

\begin{lmm}{}{} Let $p:E\to B$ be an $n$-dimensional vector bundle over $\R$. Let $E_0$ denote the zero section of $E$. Then there is a natural isomorphism $$\widetilde{H}^n(T(E);G)=H^n(E,E\setminus E_0)$$ for any abelian group $G$. 
\end{lmm}

\begin{thm}{The Thom Isomorphism}{} Let $p:E\to B$ be an $n$-dimensional vector bundle over $\R$. Let $E_0$ denote the zero section of $E$. Then there exists a unique $u\in H^n(E,E\setminus E_0;\Z/2\Z)$ such that $$u|_{(F_b,F_b\setminus\{0\})}\in H^n(F_b,F_b\setminus\{0\};\Z/2\Z)$$ is non-zero for all $b\in B$. Moreover, there is an isomorphism $$\Phi:H^k(E;\Z/2\Z)\to\widetilde{H}^{k+n}(E,E\setminus E_0;\Z/2\Z)\cong\widetilde{H^n}(T(E);\Z/2\Z)$$ given by $y\mapsto y\smile u$ for all $k\in\Z$. 
\end{thm}

Ref: Milnor



\pagebreak
\section{Cohomology of Fiber Bundles}

\begin{thm}{Leray-Hirsch Theorem}{} Let $p:E\to B$ be a fiber bundle with fiber $F$. Suppose the for some ring $R$, the following are true. 
\begin{itemize}
\item $H^n(F;R)$ is a finitely generated free $R$-module for all $n\in\N$
\item For each fiber $F$ and inclusion $i:F\to E$, there exists $c_j\in H^{k_j}(E;R)$ such that the collection $i^\ast(c_j)\in H^{k_j}(F;R)$ form a basis for the free $R$-module $H^\ast(F;R)$
\end{itemize}
Then the map $$\Phi:H^\ast(B;R)\otimes_R H^\ast(F;R)\to H^\ast(E;R)$$ defined by $\sum_{i,j}b_i\otimes_R i^\ast(c_j)\mapsto\sum_{i,j} p^\ast(b_i)\smile c_j$ is an isomorphism. 
\end{thm}

\end{document}
